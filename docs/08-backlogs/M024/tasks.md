# M024 — Tasks and evidence
Inputs: [spec](spec.md), [plan](plan.md).

## Resume
Next: none — all tasks complete. Blockers: none.
Last check: `bash scripts/backend.sh check`, `bash scripts/contract.sh check`, `python3 automation/context.py check M024` all green. Changed scope: none.

## Ordered tasks
- [x] T001 — State + persistence: add `OperationStates.Interrupted`, verify model drift (reviewed additive migration only if required); plan step 1; depends on none; done when drift checks pass with interrupted round-tripping.
- [x] T002 — Recovery service: implement `OperationRecoveryService.InterruptAsync` + startup `ReconcileOrphansAsync` (deadline-passed only) with conditional claiming, reservation release, revision advance, race-reread; plan step 2; depends on T001; done when policy unit tests pass.
- [x] T003 — Endpoints + contract/privacy: extend `OperationStatus`, map interrupted/unknown/expired reads, regenerate/review OpenAPI delta + TypeScript, auth matrix, sentinel sweep; AC-008; depends on T001, T002; done when `contract.sh check` and sweeps pass.
- [x] T004 — Recovery/restart/disconnect tests: new `OperationRecoveryTests` for AC-001/002/005/007 over file-backed SQLite with zero-dispatch counters; depends on T002, T003; done when the new tests pass.
- [x] T005 — Fencing/composition tests: AC-003/004/006 (late-outcome fencing, 409, idempotency, abort-then-success, exposure retention with original-month attribution); depends on T004; done when the new tests pass.
- [x] T006 — Regressions: full `backend.sh check`, `contract.sh check`, `context.py check M024`; depends on T005; done when all green and the completion record below is filled.

## Completion record
Executed 2026-09-11T03:46Z on .NET SDK 10.0.302 / Node v24.20.0 / npm 11.11.0, working tree at `6e86a1c0bfc3f1a5d80068a3b8046de374dc4cdd` plus the uncommitted M024 diff (runner owns commits). Environment: local run, file-backed SQLite in per-test temp dirs, `AdjustableTimeProvider` fixed at 2026-09-11T08:30:00Z unless a case advances it.

- T001: `OperationStates.Interrupted` (`"interrupted"`, 11 chars) alongside the existing pending/succeeded/failed values; the state column already stores short strings (`HasMaxLength(16)`, no check constraint), so no migration was required — `StorageMigrationTests.ModelMatchesSnapshotWithoutOpeningOrCreatingDatabase` (`HasPendingModelChanges() == false`) passes with interrupted round-tripping through the store.
- T002: new `OperationRecoveryService` (`InterruptAsync` + `ReconcileOrphansAsync`, following the `OperationSettlementService` transaction and race-reread precedent) plus `OperationRecoveryHostedService` running the bounded startup scan, both registered in `AddLinguaDeskOperations`. `InterruptAsync` is fingerprint-checked: unknown identity stays unknown, changed payload conflicts, same-state repeat observes the duplicate, non-pending records fence; the pending→interrupted claim is a conditional `UPDATE ... WHERE State = 'pending'` with reservation release on the stored admission day plus revision advance in the same short transaction (zero consumed change, monetary exposure never touched). The scan pages pending rows in insertion order (SQLite rejects `DateTimeOffset` in WHERE/ORDER BY, so the ascending-deadline expiry filter runs client-side; overflow waits for a later scan) and finalizes only deadline-passed records — unexpired pending work stays committable, so a disconnect/abort never finalizes anything by itself. Scan failure at startup logs one warning and leaves records pending (never a false terminal state); the runtime connection string is `ReadWrite` without create, so the scan cannot create a missing database file. Findings fixed during execution: SQLite `DateTimeOffset` translation (server-side comparison/order abandoned for the paged client-side filter) and the restarted test host being born at the base clock (fixture `RestartAsync` now carries the current time into `Attach` via `initialTime` before `CreateClient` starts the host, so the startup scan observes the carried-over deadline state).
- T003: `OperationStatus.Interrupted` (`"interrupted"`) with interrupted→200 status responses carrying zero character charge, fresh current-day usage and `outputAvailable: false`; same-payload resubmission of an interrupted identity returns the recorded metadata (200, no redispatch), changed payload stays 409, unknown stays 404 (`unknownOperation`, no zero-charge claim, no record), expired stays 410, malformed/future-skew stays 400 with server time. Regenerated and reviewed the OpenAPI delta (interrupted enum value + two endpoint/status descriptions) plus TypeScript declarations; `scripts/openapi-to-yaml.mjs` enum allowlist extended to `['pending', 'succeeded', 'failed', 'interrupted']` as the reviewed M024 delta. Verified/unverified/anonymous matrix on the touched status read (200/403/401 plus cross-account 404). Recovery logs carry allowlisted numerics only (scalar count, admission day, revision); no monetary minor-unit value and no source text in problems, logs or TEXT columns.
- T004: new `OperationRecoveryTests` (15 tests) prove AC-001 (direct interrupt: terminal interrupted, released reservation, zero charge, revision 2, metadata-only read + same-payload 200 resubmission; startup scan after restart with advanced clock finalizes the deadline-passed orphan), AC-002 (scan finalizes exactly the deadline-passed record while the fresh pending record stays committable and settles), AC-005 (unknown 404 asserts nothing and writes nothing, expired 410, malformed/future-skew 400 with server time) and AC-007 (same-file restart preserves interrupted state/ledgers/revision 2; post-restart duplicate interrupt idempotent and late success fenced; repeated reads leave the revision unchanged — reads never dispatch work, pinned structurally by the constructor-shape test allowing only DbContext/TimeProvider/Options/fingerprint-keys/Logger).
- T005: the same suite proves AC-003 (success and failure after interrupt fenced with no ledger/revision change, changed-payload 409 at service and HTTP layers, duplicate interrupt idempotent), AC-004 (interrupt after success fenced with the charge intact and succeeded status preserved; aborted wait commits success within the deadline with succeeded/output-unavailable status; late output never revives failed records), AC-006 (interrupted operation keeps its M023 attempt reservation `Reserved` with exposure intact and zero character charge; authoritative evidence after a 30-day advance settles 120 into the original `2026-09` month exactly once with no new month row, repeat evidence idempotent) and the concurrency case (8 parallel mixed interrupt/success attempts commit exactly one transition, revision 2, charge at most once).
- T006: `bash scripts/backend.sh check` → 446 total, 446 passed, 0 failed, 0 skipped (backend-api 165 = 150 + 15 new, backend-core 10, backend-ai 271); `bash scripts/contract.sh check` passes with zero drift; `python3 automation/context.py check M024` passes after a reviewed re-lock (intended source changes only: `Interrupted` state value, recovery service + hosted startup scan registration, interrupted status mapping, reviewed contract-validator enum update). Full diff reviewed: metadata-only records, no source/result text retention, no amounts in logs/problems/TEXT columns, no accidental artifacts.

AC verdicts: AC-001 passed, AC-002 passed, AC-003 passed, AC-004 passed, AC-005 passed, AC-006 passed, AC-007 passed, AC-008 passed. Human/live gates: none required; zero provider dispatches in this slice. Limits carried forward: no real dispatch/eligibility/fallback traversal (M026/M027), no ordered usage snapshots across periods beyond the current-day snapshot (M025); the launch cap amount, serving/billing bounds and attribution verification stay open under Q-001 pre-launch and block paid serving, not this slice.
