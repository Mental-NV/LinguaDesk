# M021 — Tasks and evidence
Inputs: [spec](spec.md), [plan](plan.md).

## Resume
Next: done; all tasks complete. Blockers: none.
Last check: `bash scripts/backend.sh check`, `bash scripts/contract.sh check`, `python3 automation/context.py check M021` green at 2026-09-11T02:53:51Z.
Changed scope: none beyond the selected slice.

## Ordered tasks
- [x] T001 — Persistence: operation/ledger entities + reviewed EF migration on the M006/M007 chain, revision counter; plan step 1; depends on none; done when model-drift/initialization checks pass on the production chain.
- [x] T002 — Admission service: identity/validity, M005 validation with pre-match defaults, HMAC fingerprint, single-transaction claim + allowance test + day stamp with violation re-read; AC-005 + policy portions of AC-001–AC-004; depends on T001; done when policy unit tests pass with no HTTP/DB/dispatch.
- [x] T003 — Endpoints + concurrency/persistence evidence: `POST /api/operations`, `GET /api/operations/{operationId}`, duplicate/conflict/expiry/allowance matrices, parallel same-user/multi-user file-backed cases, two-process restart proof, zero-dispatch counters; AC-001–AC-006; depends on T002; done when real-HTTP + file-backed cases pass.
- [x] T004 — Contract/auth/privacy evidence: C# DTOs/metadata, OpenAPI regen + TS declarations with drift failure, verified/unverified/anonymous matrix, strict-JSON matrix, sentinel sweep; AC-007; depends on T003; done when `contract.sh check` and sweeps pass with no text/secret in evidence.
- [x] T005 — Regressions and lock: `backend.sh check`, `contract.sh check`, `context.py check M021` green with revision/environment/UTC time recorded below; all ACs; depends on T004; done when every AC holds and the completion record is filled.

## Completion record
Executed 2026-09-11T02:53:51Z on .NET SDK 10.0.302 / Node v24.20.0 / npm 11.11.0, working tree at `172e160` plus the uncommitted M021 diff (runner owns commits). Environment: local macOS run, file-backed SQLite in per-test temp dirs, `AdjustableTimeProvider` fixed at 2026-09-11T08:30:00Z unless a case advances it.

- T001: `OperationSubmission` (unique `(account, operationId)`, fingerprint/metadata only), `CharacterLedgerEntry` (user/global UTC-day consumed/reserved), `LedgerRevision` singleton seeded at 0; reviewed migration `20260911024223_OperationAdmission` after the M006/M007 chain (applied migrations untouched); `StorageMigrationTests`/`StorageHostTests` chains extended per repo convention; model-drift check passes.
- T002: `OperationAdmissionService` (UUIDv7/version/variant/24h-validity/5min-skew via `ProductCatalog` constants + `TimeProvider`; M005 scalar validation with auto/mode defaults applied pre-match; canonical HMAC-SHA256 fingerprint keyed by a Data-Protection-protected persisted server key pinned to purpose `LinguaDesk.Operations.FingerprintKey/v1`; single-transaction claim + committed-plus-reserved test against configured user/global UTC-day allowances + day stamp; unique-violation re-read; expiry re-checked inside admission).
- T003: `POST /api/operations` (202 pending envelope + fresh user snapshot; 400/401/403/405/409/410/415/422/429 Problem Details per §8, `no-store`) and `GET /api/operations/{operationId}` (pending metadata or account-scoped 404, 410 past identity expiry, reads dispatch nothing); new `OperationAdmissionTests` (12 tests) prove AC-001 (8 parallel identical claims → one reservation, identical bodies), AC-002 (family/scalar/CRLF-LF/selection/target/mode changes → 409, ledger unchanged), AC-003 (reordered properties + `\uXXXX` escapes match; 25h-old identity → 410 after record cleanup; malformed/v4/future-skew → 400 with server time), AC-004 (user 100-char and global 100-char ceilings deny with 429 user/global categories, no overrun, no global counts leaked), AC-005 (401/403/400/415/422 matrix leaves zero records/reservations), AC-006 (restart on the same file + key directory preserves reservation/revision; post-restart duplicate still pending).
- T004: C# DTOs/metadata own the wire (`SubmitOperationRequest`, `OperationPendingResponse`, `UsageSnapshot`, `OperationProblemDetails`); `contract.sh generate` reviewed (purely additive: 451 YAML + 408 TS lines) and `contract.sh check` passes with the extended `openapi-to-yaml.mjs` gate (fixed wire names, no-store on every operations response, Bearer-or-cookie security); frontend `tsc --noEmit` passes; sentinel sweep finds no source/secret text in any table column, response body, or stored fingerprint; strict-JSON matrix (duplicate/unknown/non-string fields, query strings, media types, methods) green; one supporting product fix: API-path cookie challenges now return 401 instead of a login redirect (`CurrentAccountCookieEvents.RedirectToLogin`), required by AC-005; no existing redirect expectation changed.
- T005: `bash scripts/backend.sh check` → 407 total, 407 passed, 0 failed, 0 skipped (`artifacts/test-results/backend-api.trx` 126, `backend-core.trx` 10, `backend-ai.trx` 271); `bash scripts/contract.sh check` passes; `python3 automation/context.py check M021` passes after a reviewed re-lock (only intended diffs: `LinguaDeskDbContext` operation/ledger sets and the additive `05-openapi.yaml` delta). Full diff reviewed: no source/result text in records/logs/traces, allowlisted numerics only, no accidental artifacts. Findings fixed during execution: fingerprint key-file decode mismatch on re-read (caught by the restart test), `Accepts` Consumes routing fall-through for non-JSON posts (fixed with the repo's 415-fallback convention), migration-chain lists extended in both storage test files.

AC verdicts: AC-001 passed, AC-002 passed, AC-003 passed, AC-004 passed, AC-005 passed, AC-006 passed, AC-007 passed. Human/live gates: none required; zero provider dispatches in this slice. Limits carried forward: rows stay `pending` (no M022 settlement), no monetary admission (M023), no interrupted/unknown recovery mapping (M024), no cross-period attribution (M025); translation direction-pair allowlist is not enforced here (pair support stays with dispatch eligibility); the slice is not serving until M022 lands.
