# 004 — Independent AI Development: Tasks

**Version:** 1.1 · **Updated:** 2026-09-09
**State:** Complete; all execution tasks and AC-001–007 verified
**Inputs:** [spec.md](spec.md) v1.1; [plan.md](plan.md) v1.1

## 1. Ordered implementation tasks

- [x] **T001 — Preflight the selected baseline and dependency graph.** Confirm Git/source revisions, M001 completion, SDK `10.0.302`, package access/audit and existing backend/frontend commands. Add the central `Microsoft.Extensions.AI.Abstractions` 10.9.0 pin, three selected project files/solution entries and their locked dependency graphs; do not add Core, API→Ai, provider, full AI middleware or evaluation-package references. **Targets:** `backend/Directory.Packages.props`, `backend/LinguaDesk.slnx`, new Ai/test/runner project and lock files. **Depends:** none. **Checks:** AC-001/004/005; locked restore succeeds, the graph matches the plan and any indispensable access/audit failure stops dependent work.
- [x] **T002 — Implement the shared prompt snapshot and single-call boundary.** Add the embedded `eligibility.v1` instruction, typed synthetic-input composition, canonical prompt metadata/hash and narrow complete-response `IChatClient` call. Create fresh ordered messages/options per invocation, serialize source only as JSON user data, pass cancellation and return the raw response; add no parser, product classification, retry/fallback, admission, provider adapter, middleware, streaming or logging. **Targets:** `backend/src/LinguaDesk.Infrastructure.Ai/`. **Depends:** T001. **Checks:** AC-002/003/005; deterministic fixed snapshot and exactly one scripted-compatible call with the exclusions intact.
- [x] **T003 — Add focused independent-AI verification.** Implement the capturing/scripted `IChatClient` and MSTest cases for project isolation, prompt roles/order/hash/round-trip/determinism, fresh request/options, one call, cancellation and exception propagation. Guard against zero/absent/failed tests and ensure fixtures/output contain only committed synthetic text. **Targets:** `backend/tests/LinguaDesk.Infrastructure.Ai.Tests/` and focused report helpers if needed. **Depends:** T002. **Checks:** AC-001–005; V-007 selected assertions pass deterministically and a controlled test failure returns nonzero.
- [x] **T004 — Deliver the standalone inspect/probe workflow.** Implement strict `inspect` and `probe` runner modes plus `scripts/ai.sh setup|check|inspect|probe`. Use the same shared composition; keep fixed inputs, canonical output and raw/scripted labeling; reject extra/unknown arguments and propagate runner failures; target only AI projects and run offline with no API/UI/storage/live configuration. **Targets:** `backend/tools/LinguaDesk.Ai.Evaluation/`, `scripts/ai.sh`. **Depends:** T002/T003. **Checks:** AC-002–005; repeated inspect output is byte-identical, probe captures the same request once, unreachable-proxy/no-credential runs pass and negative commands exit nonzero.
- [x] **T005 — Integrate aggregate checks, documentation and regressions.** Make `scripts/backend.sh check` safe for multiple test projects with collision-free reports and separate minimum guards for the retained 42 API/storage cases and a positive AI count. Document only verified AI commands/synthetic-only limitations in README. Run locked focused AI checks plus backend check/smoke and frontend check/smoke; inspect the diff/artifacts for accidental provider, credential, database, listener, report or generated-file effects. **Targets:** `scripts/backend.sh`, `README.md`, `.gitignore` only if required, existing regression surface. **Depends:** T004. **Checks:** AC-004–007; all selected and retained suites pass with report paths/counts and controlled failures remain nonzero.
- [x] **T006 — Reconcile acceptance and close.** Review the complete diff against #0, BI-004, spec and plan. Record actual baseline/implementation revision and environment, package/resolved versions, commands/codes/counts/reports, prompt ID/hash, no-effect observations, risks/limitations and scenario outcomes below. Update #4/#6/#7/backlog/tasks/README only with supporting evidence; preserve M001–M003 history and leave M015–M020/live/product/release evidence pending. **Targets:** this record and affected planning/operating documents. **Depends:** T005. **Checks:** AC-001–007 supported before Done; otherwise leave affected state incomplete with its real blocker and remaining work.

## 2. Scenario-to-task mapping

| Scenario | Tasks | Planned evidence |
| --- | --- | --- |
| AC-001 | T001/T003/T005/T006 | Passed — three named projects, locked warning-free Release builds, isolated library graph and 10 focused tests |
| AC-002 | T002/T003/T004/T006 | Passed — repeated canonical inspect bytes, exact source JSON round trip and `eligibility.v1` resource hash below |
| AC-003 | T002/T003/T004/T006 | Passed — one captured complete-response call with fresh messages/options, supplied token and identical raw response |
| AC-004 | T001/T003/T004/T005/T006 | Passed — pinned SDK/locked commands, positive test guards, code-2 CLI failures, code-1 zero-test guard and exception case |
| AC-005 | T001/T002/T003/T004/T005/T006 | Passed — no-credential/unreachable-proxy run with no API/database/frontend/provider/report effect or retained process |
| AC-006 | T001/T005/T006 | Passed — 42 API/storage plus 10 AI cases, backend smoke, 8 frontend component and 6 published Chromium cases |
| AC-007 | T005/T006 | Passed — README commands/limitations and package/shared evidence reconciled without later-scope claims |

## 3. Completion record

**Implementation status:** Complete. **Scenario disposition:** AC-001–007 passed. **Human action/blocker:** None.

**Revision and environment:** The package was authored against Git `6f86f9f038fca908c650f5d8121a2ae53ebd8ffc`; source revisions and the clean implementation start were rechecked at planning HEAD `762a0acd9efc2fdf481b0ee1228dca722235969e`. This is the uncommitted implementation working tree based on that HEAD because milestone automation owns the commit. Verification ran on macOS 26.6 arm64 with .NET SDK `10.0.302`, runtime `10.0.10`, Node `24.20.0`, and npm `11.11.0`. No human action, provider credential, certificate, production storage, or live service was used.

**Dependency and focused evidence:** `Microsoft.Extensions.AI.Abstractions` is centrally pinned and locked at `10.9.0`; the serving library resolves no other package. `dotnet restore backend/LinguaDesk.slnx --locked-mode` and the three-project `bash scripts/ai.sh setup` exited 0. NuGet's solution-wide transitive audit reported no known vulnerable packages. The final `bash /Users/mental/Projects/LinguaDesk/scripts/ai.sh check`, invoked from `/private/tmp`, exited 0 with warning-free Release builds and 10/10 MSTest cases; [`ai.trx`](../../../artifacts/test-results/ai.trx) records 10 passed, zero failed/skipped. Credentials were removed and upper/lower-case HTTP(S)/all proxies pointed to unreachable `127.0.0.1:1`; MSTest's local IPC was allowed, but no application listener or provider client exists.

**Prompt and boundary evidence:** Repeated `inspect` executions were byte-identical. The output identifies `eligibility.v1` and raw-resource SHA-256 `4f61eb19baced2342dd1a4b27f3b08846184f202674e90243ad2a635c37eb380`, matching `shasum -a 256` over the embedded UTF-8 resource. Tests parse the user-message JSON and recover the fixed quote/newline/instruction-like source exactly while proving it is absent from the system instruction. `probe` captured the same two messages, one fresh `ChatOptions` with JSON response format, one call and the supplied cancellation token, then returned the same `ChatResponse`; output says `raw_scripted_observation`, `live: false`, and `validated: false`. A scripted exception propagated after one call.

**Failure and regression evidence:** Unknown/extra script arguments and unknown/extra runner arguments each exited 2 with usage. `LINGUADESK_AI_TEST_FILTER=FullyQualifiedName=NoSuchTest bash scripts/ai.sh check` exited 1 after the TRX guard observed zero tests; the final positive check replaced that controlled report. `bash scripts/backend.sh check` exited 0 with 52/52 cases: 42 retained API/storage in [`backend-api.trx`](../../../artifacts/test-results/backend-api.trx) and 10 AI in [`backend-ai.trx`](../../../artifacts/test-results/backend-ai.trx), with separate nonzero/failure/counter guards. `bash scripts/backend.sh smoke` passed. `bash scripts/frontend.sh check` passed 8/8 component cases, and `bash scripts/frontend.sh smoke` republished and passed 8/8 component plus 6/6 isolated Chromium cases; reports are [`frontend-unit.xml`](../../../artifacts/test-results/frontend-unit.xml) and [`frontend-e2e.xml`](../../../artifacts/test-results/frontend-e2e.xml).

**Effects, scope, and limitations:** Repository inspection found no database/sidecar, provider package/configuration, secret value, persistent evaluation report, unignored build/report output, or remaining API/runner/test process. The focused commands did not read or build frontend assets, start the API, or create storage; only ignored `bin`/`obj` and test results were generated. The API has no Ai reference, and no Core project was added. This milestone supplies only prompt inspection and a raw scripted boundary: eligibility parsing/decisions, transformations, fallback/deadline/admission, adapters/live calls, evaluation reports, quality/cost/performance, API/UI integration and every product/release gate remain pending under M015–M020 and later owning milestones.
