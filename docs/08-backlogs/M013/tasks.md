# M013 — Tasks and evidence

Inputs: [spec](spec.md), [plan](plan.md).

## Resume

Next: select and lock the next milestone. Blockers: none for M013.
Last check: contract drift passed; backend 134/134, frontend 104/104 and published HTTPS Chromium 25/25 passed with zero skips. Changed scope: the M013 frontend slice; HTTPS development/published-smoke configuration; trusted forwarded-scheme processing; a Smoke-only pre-host account seeder; browser trips; and milestone/coverage/operating records.

## Ordered tasks

- [x] T001 — Reconfirm the locked baseline, selection state, M008/M012 completion, tool pins and restore access; verify `getAccountAntiforgeryToken`, `signInLocalAccount`, `getLocalAccountSession` and `signOutLocalAccount` in the generated client and run the canonical drift check with no API change. AC-008; depends on none; done when preflight passes and the adopted shapes are current.
- [x] T002 — Implement the `/login` form, safe-return memory, session bootstrap, signed-in placeholder with inline sign out, staged `/forgot-password` state and routing guards under `frontend/src/`; wire bootstrap plus exactly `{email, password}` sign-in and sign-out posts through the generated types with flight disabling, duplicate guards, stale-completion discard, immediate teardown and in-memory-only state. AC-001–007; depends on T001; done when component behavior matches the spec contract.
- [x] T003 — Add focused Testing Library suites covering every AC-001–007 assertion without live services, sleeps or secret-bearing reports; extend the guard assertions in `App.test.tsx`. AC-001–007; depends on T002; done when the focused suite passes.
- [x] T004 — Extend the published Chromium smoke with the sign-in trip (real verified return navigation, real invalid-credential 401 with MSG-028, real 204 sign-out with no Back exposure, expiry MSG-037 entry, reload-clears, geometry) while preserving M002/M011/M012 trips. Owner-approved amendment: use an owned ephemeral HTTPS certificate and a deterministic Smoke-only pre-host Identity seeder with no test endpoint or credential output; align interactive Vite/Kestrel development on trusted HTTPS and honor a forwarded scheme only from configured trusted proxies before authentication. AC-001–004/007; depends on T002/T003; done when all published trips pass with no skips.
- [x] T005 — Run contract drift, aggregate backend/frontend gates, published smoke and dependency audits; inspect output, processes, logs and diff for echoed emails, passwords, tokens, stored credentials or side effects while preserving M001–M012 behavior. AC-008; depends on T001–T004; done when every exact plan command passes with sanitized evidence or the failing gate remains recorded.
- [x] T006 — Reconcile the diff and actual evidence against BI-013, FR-001/FR-038, UX-AC-013/014/015/016/101/102/103 and V-002/V-003/V-010/V-015; update only affected owner/status/coverage/operating rows and this backlog/tasks record, leaving M014, M009/M010, M028+, full product requirements, Q-004, P-005/P-006 and release gates pending. AC-001–008; depends on T005; done when every selected AC has reproducible evidence, local link/context audits pass and no unsupported completion claim remains.

## Completion record

Implementation and closing verification ran through 2026-09-10T13:48:13Z on
Darwin 25.6.0 arm64, .NET SDK 10.0.302, Node v24.20.0 and npm 11.11.0.
Planning baseline: `3111aea` (M013 planned); implementation worked
uncommitted on top of `3111aea`; no commit was created (the runner owns
commits). The cleartext harness proved incompatible with the locked M008
cookie contract, so the owner-approved T004 amendment was recorded in the
plan, the serving/security/seed sources were impact-reviewed and the manifest
was re-locked. The check now reports 40 selected inputs unchanged.

| Task / AC | Command or procedure | Result / evidence |
| --- | --- | --- |
| T001 / AC-008 | `python3 automation/context.py check M013`; Git/status and tool-pin inspection; `bash scripts/contract.sh check`; `getAccountAntiforgeryToken`, `signInLocalAccount`, `getLocalAccountSession`, `signOutLocalAccount` presence in `frontend/src/api/generated/linguadesk-api.d.ts` | Re-locked packet reports 40 selected inputs unchanged on planning baseline `3111aea`; the understood worktree contains only the M013 implementation and closeout. `dotnet --version` 10.0.302, `node --version` v24.20.0, `npm --version` 11.11.0. Generated antiforgery GET, sign-in (`{email, password}` → 200 signed-in with `verified`/`verificationRequired`, shared 401 `invalidCredentials`, no cookie on failure), bodyless session GET and bodyless sign-out POST adopted unchanged. |
| T002–T003 / AC-001–007 | Extended `frontend/src/api/accounts.ts` (antiforgery bootstrap, `{email, password}`-only sign-in with `X-LinguaDesk-Antiforgery` header, bodyless sign-out; 200/category/transport mapping to signed-in/invalidCredentials/retry; no logging); new `frontend/src/auth/LoginPage.tsx` (UX §9 fields, Show password with caret preservation, local validation with linked errors and first-error focus, single-flight submit with fresh bootstrap, MSG-028/MSG-037/generic states, `pageshow` password reset, in-memory-only state, exact-password pass-through); new `frontend/src/auth/safeReturn.ts` (`resolveSafeReturn` admits only `/translate` and `/rewrite`); reworked `frontend/src/shell/App.tsx` (protected-entry session bootstrap read, in-memory safe-return memory with expiry reset, verified/unverified/signed-out guards, bounded signed-in placeholder with inline Sign out following the §3.4 pending/failure/success states, staged no-form `/forgot-password` state, existing heading-focus/shell conventions kept); `VerifyEmailEntryPage` gained optional `allowUnverifiedEntry` and `protectedContinuationPath` (defaults preserve M012 behavior; continuation honors the remembered route) | Focused suites 41/41 passed (`frontend/tests/unit/login.test.tsx` 30 tests, `frontend/tests/unit/App.test.tsx` 11 tests): one bootstrap plus one `{email, password}`-only POST with safe-return navigation, heading focus, cleared password and zero language requests; flight disabling with duplicate click/Enter suppression; invalid-credential matrix with MSG-028, cleared password, kept email and email focus; unverified routing to `/verify-email` with preserved safe return through to the protected continuation; expiry teardown with MSG-037, cleared state and safe-return reset; sign-out pending/failure/retry/success matrix with auth-only requests and no Back exposure; validation no-request cases with linked errors and caret preservation; Forgot-password-during-pending discard; network-failure retry; sentinel/storage/`pageshow`/safe-return-guard cases. Report: `artifacts/test-results/frontend-unit.xml`. |
| T004 / AC-001–004/007 | `bash scripts/frontend.sh smoke` against an owned one-day loopback certificate, isolated migrated database/keys and two pre-host seeded `example.test` Identity accounts | Published HTTPS Chromium smoke passed 25/25 with zero skips: the real shared invalid-credential 401 and MSG-028 state; unverified continuation; verified sign-in to remembered `/rewrite` with heading focus and no language request; real 204 sign-out with one POST and no authenticated Back exposure; expiry MSG-037, reload clearing, validation no-request, 390px/320px reduced-motion reflow and all retained M002/M011/M012 trips. The seed process ran only under `Smoke`, exited before hosting, exposed no endpoint and wrote no credential material to seed/host/JUnit output. Report: `artifacts/test-results/frontend-e2e.xml`. |
| T005 / AC-008 | `bash scripts/contract.sh check`; `bash scripts/backend.sh check`; `bash scripts/frontend.sh smoke`; dependency audit checks; `git diff --check`; `python3 automation/context.py audit`; output/material scans | Contract drift passed unchanged (no API surface added). Backend passed 134/134 (114 API/storage, 10 Core, 10 AI). Frontend passed typecheck, lint, 104 component/policy tests (28 shared input-policy), production build and HTTPS Chromium 25/25. The committed npm/.NET lockfiles are unchanged from the prior clean audit evidence; a fresh npm advisory refresh was attempted but network isolation blocked it and external dependency-metadata egress approval was denied, so no fresh network-audit result is claimed. `git diff --check` and the 104-document link/layout audit passed. Synthetic credentials are confined to the owned harness environment and fixture source; host/seed/JUnit output, URLs, browser storage and retained Playwright artifacts contain no password/token material. M001–M012 behavior retained. |
| T006 / AC-001–008 | Full diff/schema/log/process/privacy review; owner updates (backlog, current delivery, coverage §8.2) | Diff contains the frontend sign-in slice plus the owner-approved HTTPS harness/development configuration, one-hop trusted forwarded-scheme handling, Smoke-only pre-host seeder, focused/browser tests and M013 records. There is no API operation, generated shape, migration, package dependency or auth-scheme change. BI-013 and AC-001–008 are done. M014, M009/M010, M028+, full FR-001/FR-038, Q-004, P-005/P-006 and release gates remain pending. |
