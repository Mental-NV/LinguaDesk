# M009 — Tasks and evidence

Inputs: [spec](spec.md), [plan](plan.md).

## Resume

Next: none; M009 is complete. Blockers: none.
Last check: all selected/package regression checks passed at 2026-09-10T03:11:12Z. Changed files: bearer contracts/handlers/authentication registration, generated OpenAPI/types, guarded tests/scripts, and affected coverage/delivery owners. No commit by this execution; the runner owns commits.

## Ordered tasks

- [x] T001 — Reconfirm the locked baseline, selected status, M008 completion, tool/package pins and restore access; add actual C# DTO/endpoint metadata for only bearer sign-in, bearer refresh and current-account read, generate/review OpenAPI 3.1 and TypeScript before handler adoption, and preserve zero-effect generation. AC-001/006; depends on none; done when preflight passes and reviewed artifacts contain exactly the selected shapes/security requirements with no reset/language route.
- [x] T002 — Register the real Identity bearer scheme with pinned 15-minute/7-day lifetimes and injected-time expiry; implement per-request current-account/security-stamp validation for bearer access and refresh, keep bearer-header-wins selection with no cookie fallback, and reuse the `VerifiedAccount` policy without a package or migration. AC-001–004; depends on T001; done when configuration/expiry/stamp/deletion/precedence checks pass.
- [x] T003 — Implement strict bearer sign-in, refresh and `me` under `Features/Identity/Bearer/`; share M008 strict JSON/media/error conventions; enforce non-enumeration, availability, method/media/query and secret-safe Problem Details boundaries with no cookies and no antiforgery on bearer calls. AC-001–005; depends on T001/T002; done when focused real HTTP behavior passes.
- [x] T004 — Add/organize guarded `AccountBearerTests` using migrated file SQLite, persistent keys, real Identity bearer protection/password/security stamps, HTTPS and fake time; cover every AC-001–005 assertion without test-auth handlers, mocked stores, manual tokens, sleeps or secret-bearing reports. AC-001–005; depends on T002/T003; done when the focused suite passes and the aggregate script has a positive suite-count guard.
- [x] T005 — Run contract generation/check/drift proof, aggregate backend/smoke, frontend type/build/smoke and dependency audits; inspect generated security/path semantics, token handling, pending model, processes/files and logs for forbidden effects/secrets while preserving M001–M008 behavior. AC-006/007; depends on T001–T004; done when every exact plan command passes with sanitized evidence or the failing gate remains recorded.
- [x] T006 — Reconcile the diff and actual evidence against BI-009, FR-001/FR-036, API-AC-002/003/012/014 and V-004/V-009/V-015; update only affected owner/status/coverage/operating rows and this backlog/tasks record, leaving M010/M013/M026+, full product requirements, Q-004, P-005/P-006 and release gates pending. AC-001–007; depends on T005; done when every selected AC has reproducible evidence, local link/context audits pass and no unsupported completion claim remains.

## Completion record

Completed 2026-09-10T03:11:12Z on Darwin 25.6.0 arm64, .NET SDK 10.0.302, Node v24.20.0 and npm 11.11.0. Planning baseline: `195f5d7e2b539899ebe26ad29adbef9ec7aee838` (M009 planned tree); implementation working tree remains uncommitted for the milestone runner.

| Task / AC | Command or procedure | Result / evidence |
| --- | --- | --- |
| T001 / AC-001, AC-006 | `python3 automation/context.py check M009`; Git/status and tool-pin inspection; `bash scripts/contract.sh generate`; semantic diff of `docs/05-openapi.yaml` and generated TypeScript | Lock selected inputs unchanged (40 sources); `dotnet --version` 10.0.302, `node --version` v24.20.0, `npm --version` 11.11.0. Three new operations `signInBearerClient`/`refreshBearerClient`/`getCurrentAccount`, `bearerAuth` HTTP-bearer scheme plus `bearerAuth`-or-`sessionCookie` security on `me`, strict request/success schemas; no reset/language route. Generation had no database/key/listener effect. |
| T002–T004 / AC-001–005 | `dotnet test backend/tests/LinguaDesk.Api.Tests/LinguaDesk.Api.Tests.csproj --configuration Release --filter FullyQualifiedName~AccountBearerTests` | 11/11 passed with production SQLite migrations, durable keys, real Identity bearer protection/password/security stamps, HTTPS clients and injected time. Coverage: pair shape with `expiresIn` 900/`tokenType` Bearer plus verified `verificationStatus`, `me` bearer `authMode`, unverified disclosure with fresh `VerifiedAccount` denial, invalid-credential equivalence, refresh replacement/failure matrix, both-directions precedence, no-antiforgery bearer use, exact 15-minute/7-day boundaries, stamp/deletion invalidation of access and refresh, strict request matrices, storage-loss availability, and secret sentinels. |
| T004–T005 / AC-007 | `bash scripts/backend.sh check` | 120/120 passed: API/storage 100 (incl. 11 bearer + 11 session guards), Core 10, independent AI 10. Reports: `artifacts/test-results/backend-api.trx`, `backend-core.trx`, `backend-ai.trx`. No migration: pending-model check reports no changes since the last migration. |
| T005 / AC-006 | `bash scripts/contract.sh check`; append one harmless sentinel to generated YAML; rerun check expecting failure; restore exact artifact; rerun check | Positive check passed; intentional drift exited nonzero with OpenAPI drift; restored artifact passed. Generated outputs are `docs/05-openapi.yaml` and `frontend/src/api/generated/linguadesk-api.d.ts`. |
| T005 / AC-007 | `bash scripts/backend.sh smoke`; `bash scripts/frontend.sh check`; `bash scripts/frontend.sh smoke` | Backend migrated owned SQLite and passed liveness/readiness published-host smoke. Frontend 36/36 unit/component tests and production build passed; published artifact 6/6 Chromium shell regressions passed. |
| T005 / AC-007 | `dotnet list backend/LinguaDesk.slnx package --vulnerable --include-transitive`; `npm --prefix frontend audit --audit-level=moderate` | No vulnerable NuGet packages reported; npm reported 0 vulnerabilities. |
| T006 / AC-001–007 | Full diff/schema/log/process/privacy review; sanitized TRX secret scan; `git diff --check`; `python3 automation/context.py audit`; final `python3 automation/context.py check M009` after reviewed owner/manifest update | No package/migration/account-deletion/revocation-API/reset/language/UX route added; `LinguaDesk.UnsupportedBearer` rejection replaced by the real `Identity.Bearer` scheme. Reports contain no fixture email, password, token, cookie, request token, user ID, stamp, query string or raw Identity error. Backlog/tasks/delivery/coverage rows reconciled; final audits passed. |

AC-001 through AC-007: **Passed** for the bounded M009 bearer slice. BI-009 is done. This does not complete full FR-001/FR-036, API-AC-002/003, RG-005, browser UX/privacy evidence (M013), password recovery (M010), live email (M034) or any release gate. No human/live evidence gate is selected by M009.
