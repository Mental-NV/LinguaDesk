# M022 — Tasks and evidence
Inputs: [spec](spec.md), [plan](plan.md).

## Resume
Next: done; all tasks complete. Blockers: none.
Last check: `bash scripts/backend.sh check`, `bash scripts/contract.sh check`, `bash scripts/frontend.sh check`, `python3 automation/context.py check M022` green at 2026-09-11T03:09Z.
Changed scope: none beyond the selected slice.

## Ordered tasks
- [x] T001 — Persistence: terminal outcome fields on `OperationSubmission` + reviewed additive EF migration on the M021 chain, revision reuse; plan step 1; depends on none; done when model-drift/initialization checks pass on the production chain.
- [x] T002 — Settlement service: internal `OperationSettlementService` with conditional pending→succeeded/failed transitions, admission-day ledger moves, revision advance, fencing + 409 recheck; AC-001–AC-004 policy portions; depends on T001; done when policy unit tests pass with no HTTP/DB dispatch.
- [x] T003 — Endpoints + settlement evidence: extended `GET /api/operations/{operationId}` terminal reads, duplicate/late/concurrency/midnight/restart matrices over real HTTP + file-backed SQLite, zero-dispatch counters; AC-001–AC-007; depends on T002; done when real-HTTP + file-backed cases pass.
- [x] T004 — Contract/auth/privacy evidence: extended C# status DTOs/metadata, OpenAPI regen + TS declarations with drift failure, auth matrix on the extended read, sentinel sweep; AC-008; depends on T003; done when `contract.sh check` and sweeps pass with no text/secret in evidence.
- [x] T005 — Regressions and lock: `backend.sh check`, `contract.sh check`, `context.py check M022` green with revision/environment/UTC time recorded below; all ACs; depends on T004; done when every AC holds and the completion record is filled.

## Completion record
Executed 2026-09-11T03:09Z on .NET SDK 10.0.302 / Node v24.20.0 / npm 11.11.0, working tree at `9256d46` plus the uncommitted M022 diff (runner owns commits). Environment: local macOS run, file-backed SQLite in per-test temp dirs, `AdjustableTimeProvider` fixed at 2026-09-11T08:30:00Z unless a case advances it.

- T001: `OperationSubmission` gains `State` values `succeeded`/`failed` plus nullable `SettledUtc`; reviewed additive migration `20260911030052_OperationSettlement` on the M021 chain; `StorageMigrationTests`/`StorageHostTests` chains extended per repo convention; model-drift check passes.
- T002: internal `OperationSettlementService` (test seam; no public settle endpoint) with conditional `UPDATE ... WHERE State = 'pending'` claim plus race-loss re-read, admission-day ledger moves (success: reserved→consumed on the stored day; failure: reserved decrement, consumed unchanged), durable revision advance, same-outcome idempotency, cross-outcome fencing with no writes, and fingerprint recheck for the 409 rule; shared `LedgerSnapshot` reader reused by admission and settlement; allowlisted numeric-only logs.
- T003: `GET /api/operations/{operationId}` and settled `POST` duplicates return the unified `OperationStatusResponse` (charge metadata + `outputAvailable: false` + fresh current-day snapshot, 200; pending POST duplicates stay 202); new `OperationSettlementTests` (11 tests) prove AC-001 (success commits one charge, duplicate success idempotent), AC-002 (failure releases with zero charge, duplicate failure idempotent), AC-003 (success↔failure late attempts fenced; changed payload conflicts at service and HTTP layers; same-payload POST after settlement returns 200 succeeded), AC-004 (8 parallel settlements commit one charge, revision 1→2), AC-005 (17h clock advance settles onto 2026-09-11 while the snapshot describes 2026-09-12), AC-006 (restart preserves state/ledgers/revision; post-restart duplicate idempotent and late attempt fenced; reads leave revision untouched), AC-007 (succeeded/failed/404 matrices, 410 via the unchanged identity path, unknown settles as unknown with zero writes).
- T004: C# `OperationStatusResponse` owns the terminal wire (single documented 200 schema — the generator keeps only one `Produces<T>` per status, so the earlier two-DTO shape was unified); `contract.sh generate` reviewed (succeeded/failed enum, 200-on-POST, status envelope) and `contract.sh check` passes with the extended `openapi-to-yaml.mjs` gate; auth matrix unchanged in code and covered by the M021 read tests plus the new terminal reads; sentinel sweep finds no source text in any table column, status body, or stored fingerprint; `frontend.sh check` passes (139 tests).
- T005: `bash scripts/backend.sh check` → 418 total, 418 passed, 0 failed, 0 skipped (backend-api 137, backend-core 10, backend-ai 271); `bash scripts/contract.sh check` passes; `bash scripts/frontend.sh check` passes; `python3 automation/context.py check M022` passes after a reviewed re-lock (only intended diffs: settlement service/entities/endpoints/contract plus the additive OpenAPI/TS delta). Full diff reviewed: metadata-only records, allowlisted numerics in logs, no accidental artifacts. Findings fixed during execution: contract validator pinned POST-200/status-envelope expectations, migration-chain lists extended in both storage test files, MSTest analyzer assertion forms.

AC verdicts: AC-001 passed, AC-002 passed, AC-003 passed, AC-004 passed, AC-005 passed, AC-006 passed, AC-007 passed, AC-008 passed. Human/live gates: none required; zero provider dispatches in this slice. Limits carried forward: no monetary admission (M023), no interrupted/unknown recovery mapping or output delivery (M024), no cross-period snapshot ordering (M025), no real dispatch/fallback (M026/M027); terminal `interrupted`/`unknown` states do not exist yet.
