# M023 — Tasks and evidence
Inputs: [spec](spec.md), [plan](plan.md).

## Resume
Next: none — all tasks complete. Blockers: none.
Last check: `backend.sh check` 431/431, `contract.sh check` pass, `context.py check M023` pass (2026-09-11T03:25Z). Changed scope: none.

## Ordered tasks
- [x] T001 — Persistence: add `MonetaryCostLedger` + `MonetaryAttemptReservation` entities, `LinguaDeskDbContext` sets/configuration, reviewed additive EF migration; AC-007 (durable rows); depends on none; done when model-drift/initialization checks pass.
- [x] T002 — Options: add `MonetaryAdmissionOptions` + startup validator, wire local/test configuration with small explicit caps; spec §Constraints; depends on none; done when options-validation unit tests pass.
- [x] T003 — Admission: implement ceiling check-and-reserve transaction, cost-month stamp, `BillingProfile` upper-bound computation with ceiling rounding, ineligible path, duplicate observation, plus file-backed SQLite tests for AC-001/002/003/006/007; depends on T001, T002; done when new `MonetaryAdmissionTests` pass.
- [x] T004 — Reconciliation: implement settle-once/release transaction with evidence rules, plus tests for AC-004/005 and the settlement half of AC-006; depends on T003; done when reconciliation tests pass.
- [x] T005 — Endpoints + contract/privacy: map denial/ineligible outcomes to 503/422 problem shapes with no monetary values, regenerate/review OpenAPI delta + TypeScript if shapes change, sentinel sweep; AC-008; depends on T003, T004; done when `contract.sh check` and sweeps pass.
- [x] T006 — Regressions: full `backend.sh check`, `contract.sh check`, `context.py check M023`; depends on T005; done when all green and the completion record below is filled.

## Completion record
Executed 2026-09-11T03:25Z on .NET SDK 10.0.302 / Node v24.20.0, working tree at `8f8005e` plus the uncommitted M023 diff (runner owns commits). Environment: local macOS run, file-backed SQLite in per-test temp dirs, `AdjustableTimeProvider` fixed at 2026-09-11T08:30:00Z unless a case advances it.

- T001: `MonetaryCostLedger` (CostMonth PK `yyyy-MM`, known spend + unresolved exposure in minor units) and `MonetaryAttemptReservation` (unique AttemptId, operation reference, attempt number, bound, cost month, tariff reference, reserved/settled/released state, settled actual, evidence reference, timestamps) with the M021/M022 key-length conventions; reviewed additive migration `20260911032126_MonetaryAdmission` on the M022 chain; `StorageMigrationTests`/`StorageHostTests` chains extended per repo convention; model-drift check passes.
- T002: `MonetaryAdmissionOptions` (`SectionName = "MonetaryAdmission"`: positive `MonthlyCapMinorUnits`, non-empty `Currency`; defaults unconfigured so validation fails closed) plus `MonetaryAdmissionOptionsValidator` following the `OperationAdmissionOptionsValidator` precedent, registered with the scoped `MonetaryAdmissionService`; tests use small explicit caps via factory overrides. Invalid config denies paid admission (`ConfigurationInvalid`, no writes) and `IOptions.Value` throws `OptionsValidationException`.
- T003: `MonetaryAdmissionService.AdmitAsync` checks `known(current month) + unresolved(all months) + new bound <= cap` and reserves in one short transaction whose first statement is `INSERT OR IGNORE` on the month ledger (write-lock serialization, not an in-process lock alone; bounded retry on SQLITE_BUSY/LOCKED). Cost month comes from the server transaction clock and is immutable once reserved. The upper bound is computed from an `AttemptCostProfile` projection (peak cache-miss input rate over max input tokens plus peak output rate over max output tokens, each ceiling-rounded, plus other billable charges; integer minor units, never floating-point accumulation). The projection keeps `LinguaDesk.Api` decoupled from `Infrastructure.Ai` (no new project reference); M026/M027 map `BillingProfile`+`ContextBounds` onto it. Unknown/missing/unbounded/currency-mismatched/negative input is ineligible with no writes; duplicate attempt identity observes the stored reservation (unique-violation race included). New `MonetaryAdmissionTests` (13 tests) prove AC-001 (fit admits 150, over-cap 900 denied with `monetarySuspended`, no writes on deny), AC-002 (seven ineligible shapes, zero rows), ceiling rounding (0.5→1 per leg), AC-003 (8 parallel admissions admit exactly 3×300=900 ≤ 1000, 5 denied, ledger invariant holds), AC-006 admission half (30-day advance: 800 prior exposure blocks 300, admits 200 in `2026-10` with a new known-spend bucket), and AC-007 (same-file restart + re-migrate observes the duplicate with exposure unchanged).
- T004: `ReconcileAsync` claims via conditional `UPDATE ... WHERE State = 'reserved'`, then moves exposure→known-spend (settled actual capped at the reservation) or decrements exposure (release, floored at zero) in the reservation's original cost month within the same short transaction; non-reserved rows return the stored outcome, so repeated evidence changes nothing. Missing/conflicting/negative evidence retains the full reservation. Tests prove AC-004 (settle 800→500 known/0 exposure, repeat idempotent, release clears 200 with known unchanged, unknown attempt), AC-005 (three retained shapes, exposure intact), and the AC-006 settlement half (September admission settled after rollover attributes 800 to `2026-09`; no `2026-10` row created).
- T005: `MonetaryAdmissionProblem` maps denial/unconfigured to 503 `monetarySuspension` and ineligible to 422 `monetaryIneligible` with static amount-free copy; no DTO/endpoint/OpenAPI change in this slice (no dispatch exists), so `contract.sh check` passes with zero drift and the wire names stay documented here until M026/M027 endpoints finalize them. The constructor-shape test pins zero dispatch dependencies (DbContext/TimeProvider/Options/Logger only). The sentinel sweep proves no minor-unit value appears in logs, problem payloads, or any TEXT column, and identity sentinels appear only in `MonetaryAttemptReservations` columns.
- T006: `bash scripts/backend.sh check` → 431 total, 431 passed, 0 failed, 0 skipped (backend-api 150, backend-core 10, backend-ai 271); `bash scripts/contract.sh check` passes; `python3 automation/context.py check M023` passes after a reviewed re-lock (only intended source change: the additive `LinguaDeskDbContext` extension exactly per plan step 1). Full diff reviewed: metadata-only records, no amounts in logs, no accidental artifacts. Findings fixed during execution: Api↔Ai decoupling via the cost-profile projection, CA1873 eager-logging args, privacy sweep scoped to TEXT columns (INTEGER amount columns hold minor units by design), migration-chain lists extended in both storage test files, MSTest analyzer assertion forms.

AC verdicts: AC-001 passed, AC-002 passed, AC-003 passed, AC-004 passed, AC-005 passed, AC-006 passed, AC-007 passed, AC-008 passed. Human/live gates: none required; zero provider dispatches in this slice. Limits carried forward: no real dispatch/eligibility/fallback traversal (M026/M027), no interrupted/unknown recovery mapping or output delivery (M024), no ordered usage snapshots across periods (M025); the launch cap amount, serving/billing bounds and attribution verification stay open under Q-001 pre-launch and block paid serving, not this slice.
