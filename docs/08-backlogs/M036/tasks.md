# M036 — Tasks and evidence
Inputs: [spec](spec.md), [plan](plan.md).

## Resume
Next: T005 closeout below (report, regressions, sweep, re-lock). T001–T004 complete; no owner action outstanding.
Last check: `bash scripts/ai.sh check` passed 2026-09-14 (311/311, `artifacts/test-results/ai.trx`); `bash scripts/backend.sh check` passed (547/547). Changed scope since T001: B1 re-pinned (`245b8f76…`), new `batch-security.json` (`2f22484f…`, `bsec-` IDs), per-batch pin support in loader/runner, failure fixes, judge registration + judge runner below, calibration pack below.

## Ordered tasks
- [x] T001 — Batch-input runner (`evaluate-batch`, hash gate, scripted offline B1, `ai.sh` surface); AC-001/AC-002; depends on none; done when scripted B1 passes offline with zero dispatches and a hash-mismatch fixture blocks.
- [x] T002 — Budgeted live B1 run (`verify-access` + `evaluate-batch --live`, admitted dispatches/spend/deadline, quiescence recorded); AC-002; depends on T001 + owner budget/weakened-truth acceptances; done when all 24 rows are `live_qualification` within budget. DONE 2026-09-13 23:27–23:28 UTC: 24/24 `live_qualification`, 24 `Succeeded`, 0 mismatches/flags, hash verified on current `245b8f76…` pin, 48 dispatches / actual $0.0125 of admitted 120/$8.00. (Three earlier B1 runs that day reported `fail` with the same 2 bound-related rows; fixed and superseded — see evidence.) SECURITY live run done separately (24/24 `live_qualification`, 37 dispatches, actual $0.0075).
- [x] T003 — Pinned-judge grading (blinded labels, calibration record, bounded retries); AC-003; depends on T001 (offline proof) and T002 (live grades); done when every successful output carries a reproducible grade record. DONE 2026-09-15: `artifacts/batch/evaluate-batch-20260915T165751Z-live-graded.json` — status success, 24/24 `live_qualification` Succeeded, 24 resolved model grades under the accepted `qwen-batch-judge-calibration.v1` record, zero critical flags, 120/132 dispatches, actual $0.0288, unresolved $0. (First graded attempt halted at 21 rows on under-admitted 96 dispatches; retained as failure evidence, superseded.) IMPLEMENTATION COMPLETE / LIVE EVIDENCE BLOCKED: Qwen 3.8 Flash is pinned as judge; `batch-judge.v2` sends blinded source/reference/output evidence, accepts only the exact four-score/closed-critical JSON envelope, shares the candidate `EvaluationBudget`, retries an invalid envelope exactly once, and leaves missing/invalid/transport grades unresolved. Live admission rejects absent or invalid human calibration (at least 40 development cases, at least 10 each en/ru/ro/zh, release-ID/source-disjoint, exact judge/prompt hashes, identified reviewers independent of the judge). Offline B1 passes with 24/24 reproducible scripted grade records and zero provider dispatches; focused judge/calibration/review-selector tests pass. The owner-run access and classification-envelope checks passed, but they are not B1 grading evidence. T003 remains unchecked until an actual accepted calibration and graded live report exist. Owner ran the live calibration draft 2026-09-15 into `artifacts/judge/calibration-pack/live-run/` — 37/40 rows usable; 3 rows `DeadlineExceeded` (5s release eligibility stage timeout vs provider-latency spikes; transient, rerun advised) and all pack files show `\uXXXX` escapes (serializer default; fixed post-run via relaxed escaping + `Policy` snapshot + stage-timeout override flags in `calibrate-judge`, `ai.sh check` 312 green). Rerun pending; no labeling yet (scores unseen by reviewers is still intact — the escaped draft was never labeled). 2026-09-15: owner reran to `live-run2/` (40/40 Succeeded, default policy snapshot recorded), labeled blind, and ran finalize → `artifacts/judge/qwen-calibration-accepted.json` written (40 cases, 10/language, exact 143/160 = 89.4%, within-one 155/160 = 96.9%, critical-set 37/40, no sub-60% warning). Script fixes that day: PascalCase/camelCase label tolerance (was KeyError) and resolved-grade (not release-Usable) validity gate — the one sub-2 row (cal-rw-ru-correctionOnly-01, judge meaning 0 vs human 1) is valid low-end data under the 0–3 contract. 2026-09-15 owner waiver (this request): T003 calibration labeling may come from an independent frontier LLM — bilingual-human staffing is unrealistic for this scope and unnecessary here. Gate relaxed the same day: reviewers must be identified with competence statements and independent of judge and candidate — judge-independence mechanically enforced in gate + script, candidate-independence in the script (the accepted record carries no candidate ID, so live admission takes the owner's attestation); messages/spec/README/protocol updated, `CalibrationRejectsJudgeAsReviewer` test added, `ai.sh check` green. The `ai-gpt-5.6-sol` record (independent of DeepSeek candidate and Qwen judge, identity/competence disclosed) satisfies the relaxed gate. Remaining for T003: stamp the limitation placeholders with measured agreement (actuals 89.4/96.9) and run the graded live B1 report. 2026-09-15 graded attempt `evaluate-batch-20260915T163945Z-live-graded.json` HALTED: 19/21 rows Succeeded with resolved grades, 2 ro-rewriting rows budget-denied with zero provider calls, 3 zh-rewriting rows never ran — 96 admitted dispatches covered only 21 rows because each graded row costs 5 counts (2 stage reservations + 3 sends; 19×5+1=96 exactly). Protocol step 5 corrected to 132 dispatches; rerun pending.
- [x] T004 — Human output review (seeded 12-of-24 + all flags/mismatches, adjudication, disposition record); AC-004/AC-006; depends on T002/T003; done when disposition is recorded with identity/competence/rationale. DONE 2026-09-15: `artifacts/batch/t004-disposition.json` — owner (r-owner, claimed EN/RU/RO/ZH competence) reviewed all 16 packet cases (12 preselected + 4 sub-3 extras) with per-case agree verdicts + rationales; zero disputes (adjudication vacuous), zero suspected critical errors, disposition `accept` (recorded as AC-006 `accepted`). The runner now selects and records a deterministic 12-case, 6 Translation / 6 Rewriting sample from an owner seed before output generation, enforcing both Chinese scripts, shortest/longest quartiles and all pinned fidelity-risk tags. Owner reviewed SECURITY live outputs 2026-09-13 ("look good"; 11 guardrail refusals accepted as correct) — informal disposition, not the seeded review.
- [x] T005 — Versioned §7.2 report, regressions (`ai.sh check`, backend check, `context.py check M036`), secret/production-text sweep; AC-005/AC-006; depends on T004; done when all pass and the sweep is clean. DONE 2026-09-15: report `artifacts/batch/m036-report-v1.md` (manifest/cases/aggregation/spend/disposition/retained-failures/limitations); `ai.sh check` 313/313; `backend.sh check` 549/549; `context.py check M036` clean after re-lock; sentinel sweep clean (0 matches over changed code, corpora, reports, labels, disposition, docs; graded report carries only credentialRef + presence flags).

## Completion record
T001 complete 2026-09-13 UTC. New code (no prompt/checker/adapter/registry edits; runner-only, no serving change):
- `backend/src/LinguaDesk.Infrastructure.Ai/BatchCorpus.cs` — current frozen B1 pin (`245b8f76…`, schema `corpus-b1.v1`, 24 cases), hash gate, loader, digit-token finding.
- `backend/tools/LinguaDesk.Ai.Evaluation/EvaluateBatch.cs` — `evaluate-batch` (hash gate → exit 3 before any dispatch; primary-only `ChainOrchestrator` traversal with §6 policy 5s/10s/2s inside `--deadline-ms` default 30000; `offline_fixture` scripted rows vs `live_qualification` rows never mixed; missing credential → blocked exit 3; budget-exhaustion halt without exceeding caps).
- `backend/tools/LinguaDesk.Ai.Evaluation/BatchJudge.cs` — pinned runner-only judge `batch-judge.v2` and `batch-grading-prompt.v2`; blinded labels, strict four-dimension 0–3 envelope, closed critical-error vocabulary, exact one-retry ceiling, judge identity/usage/spend records and unresolved-on-failure behavior. Human review remains authoritative.
- `backend/tools/LinguaDesk.Ai.Evaluation/JudgeCalibration.cs` — rejects live grading before dispatch unless an explicitly accepted, independently labeled, 40-case minimum calibration record matches the selected Qwen profile and exact prompt revision/hash, is disjoint from B1, and has no reviewer identical to the judge.
- `backend/tools/LinguaDesk.Ai.Evaluation/BatchReviewSelector.cs` — deterministic seeded 12-of-24 selection recorded before output generation with the required family/script/length/risk strata.
- `backend/tests/LinguaDesk.Infrastructure.Ai.Tests/BatchCorpusTests.cs` — 5 offline tests (hash pin, tamper rejection, 24-case load, schema rejection, digit finding).
- `scripts/ai.sh` + runner README — `evaluate-batch` surface (offline dead-ended by default, `--live` passthrough).

Evidence (all UTC 2026-09-13, .NET SDK 10.0.302):
- `bash scripts/ai.sh check` → pass, 286/286 including 5 new `BatchCorpusTests` (`artifacts/test-results/ai.trx`).
- `bash scripts/backend.sh check` → pass, 522/522.
- Offline scripted B1: exit 0, status `pass`, 24/24 `Succeeded`, `offline_fixture` only, provider dispatches 0, spend 0, hash verified `245b8f76…`; every row carries a reproducible `batch-judge.v2` scripted-grade record with blinded label and explicit fixture calibration ref. This proves the contract only, not live quality.
- Hash-mismatch fixture (one flipped byte): exit 3, status `blocked`, 0 rows, 0 dispatches.
- Missing-credential `--live` probe (credential env stripped, dead-ended proxy): exit 3, status `blocked`, 0 dispatches — never scripted substitution.
- Secret/production-text sweep over new code + offline report: clean (only `CredentialRef` + presence flags; outputs are synthetic scripted strings, no production text).
- Re-pins by owner request 2026-09-13: B1 `981e340b…` (rewriting error-injection + `b1-tr-ru-ro` metadata fix; all corpus tests green); SECURITY `2f22484f…` (new guardrail batch, `bsec-` IDs, counts/operations/review blocks verified, zero source overlap with B1).
- New code 2026-09-13: `BatchCorpus` accepts `SECURITY` batchId (`FrozenPinForBatchId`, `ReadBatchId`, per-batch `IsFrozenRevision`); `EvaluateBatch` resolves the pin per batch for gate + report; `BatchCorpusTests` +2 (security pin, security loader). Corpus test classes 17/17 green.
- Live SECURITY run 2026-09-13 22:17–22:18 UTC (`artifacts/batch/evaluate-batch-20260913T221811Z-live.json`): exit 0, `success`, hash verified, 24/24 `live_qualification`, 37 dispatches, actual $0.0075 (reserved $0.0423) of admitted 120/$8.00; 13 `Succeeded`, 11 `Ineligible/refused` (one eligibility dispatch each — model refused hostile payloads at the gate); 11 `unexpected-eligibility-outcome` flags (corpus marks all `eligible`; no expected-refusal convention yet).
- Live B1 runs 2026-09-13 (`evaluate-batch-20260913T200948Z`, `T205409Z`, `T210554Z` `-live.json`): each exit 1, `fail`, hash verified against the then-current pin (`aebdc2dc…`, `576fc5…`, `981e340b…` respectively), 24/24 `live_qualification`, 45 dispatches / actual ~$0.0088 each of admitted 120/$8.00; 22 `Succeeded`, 2 `Failed/provider-failure` in all three runs (`b1-tr-ro-zh`: provider answered then failure; `b1-rw-ru-academic`: 0 ms, zero provider attempts). Owner reviewed the 22 successful B1 outputs ("look good") — informal disposition, not the seeded T004 review; the 2 failed rows had no output to review.
- Failure fixes 2026-09-13 (root causes were client-side bounds, masked by blanket `provider-failure` rows): `b1-rw-ru-academic` shortened 1498→682 chars (all digits/observations kept; B1 re-pinned `245b8f76…`); `MaxOutputTokens` 256→4096 on both profiles (the 4235-char `ro-zh` translation cannot fit 256 output tokens); adapter input estimate now script-aware (ASCII/4, two-byte scripts/2, CJK/1) instead of escaped-JSON bytes, which had overcounted Cyrillic ~6×; pipelines now report the real `AttemptFailureKind` (`length-rejected`, `timeout`, …) via `ChatCompletionsAdapter.FailureCategory`. Tests: estimator regression, kind-mapping, 4096-assertion added; reservation helper derives from registry bounds. Offline `evaluate-batch` passes for both corpora on the new pin.
- Judge-model registration 2026-09-14: kept `Muse-Spark-1.3-Contributor` and added selected judge `Qwen-Qwen3.8-Flash` (`qwen/qwen3.8-flash`) through OpenRouter. Both use purpose-scoped ref `judgment`, 8,192 input / 10,000 output policy caps, 65,536 response bytes, 60s timeout and low reasoning effort. Verified OpenRouter catalog values: Qwen context 1,000,000 tokens and 0.15/0.47 USD/M input/output; endpoint records have identical prices, with routed context 262,144 (Makora) or 1,000,000 (Alibaba), while the enforced 18,192-token combined cap fits both. Muse retains its verified 1,048,576 context and 0.10/0.20 rates. DeepSeek stays at 8,192/4,096 and its existing rates. Exact conservative per-dispatch reservations: both DeepSeek profiles `(8192×0.30 + 4096×1.20)/1M`, rounded up, = $0.007373 (the earlier requested $0.0056 example is arithmetically incorrect); Muse `(8192×0.10 + 10000×0.20)/1M` = $0.002820; Qwen `(8192×0.15 + 10000×0.47)/1M` = $0.005929. All are below $0.20, all input+output sums fit capacity, and 10,000 tokens × approximately 4 bytes/token = approximately 40 KB below the 65,536-byte cap. Adapter/conformance now preserve DeepSeek's existing `thinking: {"type":"disabled"}` body with no `reasoning`, while OpenRouter carries `reasoning: {"effort":"low"}` with no `thinking`; reasoning efforts are closed-set validated. `scripts/ai.sh` strips both `JUDGMENT` and legacy `MUSE` credential variables offline. Focused suites pass; full suites 309/545 green (re-verified here). FamilyChain, DeepSeek bounds, corpora/pins and existing billing rates untouched; `BatchJudge.cs` was extended (model-backed `GradeAsync`, v2 prompt), not untouched. Live evidence in `artifacts/judge/`: early runs `blocked` (key not yet exported), then `ProviderFailure HTTP 404`, then `qwen-verify-access-20260914T170509Z.json` succeeded (1 dispatch, returned model `qwen/qwen3.8-flash`, known usage, $0.00011604 actual, zero unresolved); `qwen-live-classification-20260914T170540Z.json` succeeded (12/12 reference-matching, 10 dispatches, $0.00116941 reserved, zero unresolved, `judgment` ref). So the judge path is live-proven for access + classification envelopes; graded B1 still awaits an accepted calibration.
- Live B1 clean run 2026-09-13 23:27–23:28 UTC (`artifacts/batch/evaluate-batch-20260913T232801Z-live.json`): exit 0, `success`, hash verified on `245b8f76…`, 24/24 `live_qualification`, 24 `Succeeded`, 0 mismatched, 0 flags, 48 dispatches, actual $0.0125 (reserved $0.2443 — higher reservations reflect the 4096-token output cap) of admitted 120/$8.00. Confirms both failure fixes; T002 done-condition met.
- Calibration work pack 2026-09-14 (`artifacts/judge/calibration-pack/`, gitignored): `dev-corpus.json` (40 synthetic sources, 10/language, ID/source-disjoint from both release batches, sha `30820e0c…`), new `calibrate-judge` command (candidate chain + judge grading → `calibration-draft.json` + blind `review-pack.json` + labels template; exit 3 on missing credential, no silent defaults), `finalize-calibration.py` (assembles the accepted record, enforces every gate rule incl. release disjointness, prints agreement), `reviewing-protocol.md` (owner steps). Tests: offline pack generation + malformed-corpus usage error. Suites 311/547 green; offline smoke run 40/40 clean with blinding verified.

Human/live gates still pending (blockers, not waived — only an explicit owner decision can waive, never silence):
1. Owner admits the live budget and the weakened-ground-truth limitation per run (SECURITY: 37 dispatches / $0.0075; B1 clean run: 48 dispatches / $0.0125 — plus three superseded B1 runs at ~45 dispatches / ~$0.0088 each; every run stayed within its admitted 120/$8.00).
2. Bilingual/fluent reviewers + adjudicator (seed recorded before results are visible: 12-of-24 stratified, both Chinese scripts) before T004/disposition.
3. Serving-quiescence (or shared-scope concurrency) statement recorded alongside each live run (runner records `unverified-standalone-shared-credential` by default; still open for the SECURITY run).
Live dispatches made for SECURITY (37) and B1 (3×45 superseded + 48 clean); total actual ~$0.046, each run within its admitted 120/$8.00; no per-direction 90% claim; G1 gap and successor work (full corpus, full human coverage, model judge calibration, M037 workloads) unchanged.

AC status: AC-001 runner mechanics proven offline + live (both batches consumed with hash verified); AC-002 done (clean B1 live run 23:28 UTC); AC-003/AC-004/AC-005/AC-006 pending model grades, formal human review, versioned filing and disposition.

`context.py check M036` 2026-09-14 lists changed/unlocked `spec.md` (B1 pin follow-up, done correctly), `docs/delivery/current.md`, `Corpus/batch-b1.json`, runner `README.md` + `Program.cs`; lock intentionally not refreshed. A delegated hand-edit of machine-managed `context.json` plus invented CLI/docs claims were reverted; behavior is covered by the 309-test suite and the probes above. #6 coverage: no row change — no new product behavior or scenario mapping yet; V-008/V-013 batch evidence remains partial until graded live run + review complete.
