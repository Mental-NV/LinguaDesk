# M012 — Tasks and evidence

Inputs: [spec](spec.md), [plan](plan.md).

## Resume

Next: done; no further tasks. Blockers: none.
Last check: all ordered tasks passed. Changed scope: none.

## Ordered tasks

- [x] T001 — Reconfirm the locked baseline, selection state, M007/M011 completion, tool pins and restore access; verify `confirmLocalAccountEmail`, `resendLocalAccountVerification` and `getLocalAccountSession` in the generated client and run the canonical drift check with no API change. AC-008; depends on none; done when preflight passes and the adopted shapes are current.
- [x] T002 — Implement the `/verify-email` status/resend/link-consumption/continuation page and routing changes under `frontend/src/`; wire exactly `{email}`, `{userId, code}` and the bodyless session GET through the generated types with flight disabling, duplicate guards, stale-completion discard, query stripping and in-memory-only state. AC-001–007; depends on T001; done when component behavior matches the spec contract.
- [x] T003 — Add focused Testing Library suites covering every AC-001–007 assertion without live services, sleeps or secret-bearing reports; extend the redirect/no-material assertions in `App.test.tsx`. AC-001–007; depends on T002; done when the focused suite passes.
- [x] T004 — Extend the published Chromium smoke with the verification trip (real invalid-link 400, real resend 202, no-material routing, reload-clears, geometry) while preserving M002/M011 trips; attempt the real-API valid-link trip only without a new test endpoint. AC-001–003/006–007; depends on T002/T003; done when the browser trip passes against the owned loopback host or the valid-link gap is recorded.
- [x] T005 — Run contract drift, aggregate backend/frontend gates, published smoke and dependency audits; inspect output, processes, logs and diff for echoed user IDs, codes, stored credentials or side effects while preserving M001–M011 behavior. AC-008; depends on T001–T004; done when every exact plan command passes with sanitized evidence or the failing gate remains recorded.
- [x] T006 — Reconcile the diff and actual evidence against BI-012, FR-002, UX-AC-008/009/010/102/104 and V-003/V-004/V-010/V-015; update only affected owner/status/coverage/operating rows and this backlog/tasks record, leaving M013–M014, M034, full product requirements, Q-004, P-005/P-006 and release gates pending. AC-001–008; depends on T005; done when every selected AC has reproducible evidence, local link/context audits pass and no unsupported completion claim remains.

## Completion record

Implementation and closing verification completed 2026-09-10T11:08:22Z on
Darwin 25.6.0 arm64, .NET SDK 10.0.302, Node v24.20.0 and npm 11.11.0.
Planning baseline: `611085a3fb90ae9bfe72fee8cbed19eb3931e060`; implementation
worked uncommitted on top of `304c95e` (M012 planned); no commit was created
(the runner owns commits). `python3 automation/context.py check M012` reports
the 34 selected inputs unchanged.

| Task / AC | Command or procedure | Result / evidence |
| --- | --- | --- |
| T001 / AC-008 | `python3 automation/context.py check M012`; Git/status and tool-pin inspection; `bash scripts/contract.sh check`; `confirmLocalAccountEmail`, `resendLocalAccountVerification`, `getLocalAccountSession` presence in `frontend/src/api/generated/linguadesk-api.d.ts` | Lock reports the 34 selected inputs unchanged; clean tree on top of `304c95e`; `dotnet --version` 10.0.302, `node --version` v24.20.0, `npm --version` 11.11.0. Generated confirm (`{userId, code}` → 200 `verified` / generic 400), resend (`{email}` → fixed 202 `retryAfterSeconds: 60`) and bodyless session GET adopted unchanged; no generation or handler work. |
| T002–T003 / AC-001–007 | Extended `frontend/src/api/accounts.ts` (resend/confirm/session `fetch` wrappers sending exactly `{email}`, `{userId, code}` and a bodyless GET; 202/200/status mapping to sent/verified/invalid/field/retry; no logging); reworked `frontend/src/auth/VerifyEmailEntryPage.tsx` (in-memory status with email, server-driven resend countdown via `retryAfterSeconds`, single-read `I’ve verified my email` status check, once-only link consumption with immediate history replacement, MSG-032 invalid path with resend action, signed-out/signed-in continuation variants, generic network retry, per-action flight guards with stale-completion discard, status-region focus on state change, in-memory-only state); `frontend/src/shell/App.tsx` guarded entries (`/`, `/translate`, `/rewrite` → `/verify-email` while an unverified registration is pending, else `/login`) | Focused suites 28/28 passed (`frontend/tests/unit/verify-email.test.tsx` 17 tests, `frontend/tests/unit/App.test.tsx` 10 tests): in-memory email status with guarded protected navigation and zero transformation requests; one `{email}`-only resend with MSG-035, server-cooldown disable/countdown/reenable and focus move; in-flight duplicate suppression; no-request invalid-email case with linked errors and field focus; one `{userId, code}`-only confirm POST with query stripping, signed-out continuation and sentinel absence; session-read protected continuation with a single bodyless GET; invalid/expired/malformed matrix with MSG-032 plus resend path and zero confirm posts for malformed material; still-unverified guidance after one session read; generic failure retry with one-request retry and no false success; resolve-after-navigation discard with no redirect or query restore; no-material direct entry back to `/register`; Enter-once keyboard path with native controls; delivered-material absence from storage/history/URL/body. Report: `artifacts/test-results/frontend-unit.xml`. |
| T004 / AC-001–003/006–007 | `bash scripts/frontend.sh smoke` (owned loopback host, isolated database/keys, synthetic `example.test` addresses) with new `frontend/tests/e2e/verification.spec.ts` | Published Chromium smoke 17/17 passed (6 retained M002 shell trips, 5 retained M011 registration trips, 6 new verification trips): delivered synthetic link posts exactly once to the real `/api/accounts/confirm-email`, returns the real generic 400, strips the query and shows MSG-032 with the resend path; real registration followed by one `{email}`-only resend returns the real 202 with MSG-035, disabled control and server countdown; direct no-material entry routes to `/register`; invalid-link query stripped on read with reload clearing back to `/register`; unverified Translation/Rewriting navigation stays on verification with no language requests; 390px/320px verification reflow with reduced motion and unclipped controls. Report: `artifacts/test-results/frontend-e2e.xml`. |
| T005 / AC-008 | `bash scripts/contract.sh check`; `bash scripts/backend.sh check`; `bash scripts/frontend.sh check`; `bash scripts/frontend.sh smoke`; `npm --prefix frontend audit --audit-level=moderate`; `dotnet list backend/LinguaDesk.slnx package --vulnerable --include-transitive`; `git diff --check`; `python3 automation/context.py audit`; sentinel scans of reports/sources | Contract drift check passed unchanged (no API surface added). Backend 134/134 passed (114 API/storage, 10 Core, 10 AI). Frontend gate passed: typecheck, lint, 73 component/policy tests (28 shared input-policy), production build. Published smoke 17/17. npm audit 0 vulnerabilities; .NET audit no vulnerable packages. `git diff --check` clean; context link/layout audit passed (100 Markdown documents). Synthetic material absent from storage/history/URL-after-read, response bodies excluded from assertions, test reports and shipped sources; no credential side effects; M001–M011 behavior retained. |
| T006 / AC-001–008 | Full diff/schema/log/process/privacy review; owner updates (backlog, current delivery, coverage §8.1/§8.2); `python3 automation/context.py audit` | Diff scoped to the frontend verification slice (API wrappers, verification page, guarded shell entries) plus focused tests and M012 records; no API, migration, package, auth-scheme or persistence change. BI-012 done with every selected AC reproducibly evidenced; M013–M014, M034, full FR-002, Q-004, P-005/P-006 and release gates remain pending. |

AC-001 through AC-008: **passed** with the evidence above for the bounded M012
web-verification slice; BI-012 is implemented and done. Recorded gap: real-API
valid-link browser success was not exercised because no delivered confirmation
material can be sourced through the owned smoke host without adding a test-only
endpoint; AC-003 is carried by component single-POST/continuation evidence plus
the real-API invalid-link browser path. No human/live evidence gate is selected
by M012. This does not complete sign-in/out or safe-return routing (M013),
recovery forms (M014), live email delivery/templates/origin (M034), full FR-002
or any release gate.
