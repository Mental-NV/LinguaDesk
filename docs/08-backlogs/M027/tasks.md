# M027 — Tasks and evidence
Inputs: [spec](spec.md), [plan](plan.md).

## Resume
Next: review and handoff; all implementation tasks and package checks pass. Blockers: none.
Last check: `bash scripts/verify-milestone.sh M027` passed all gates at 2026-09-11T16:35Z (context check + audit, backend check, backend smoke, contract check, AI check + probe, frontend check + 38 published browser tests, `git diff --check`). Context re-locked twice after review: spec/plan owner-alignment (`correctionOnly`/`rewriting` wire names per ProductCatalog/contract; no scope change), then closeout reference updates. Changed scope: owner-alignment + two reviewed implementation deltas below; no new product policy.
Changed scope detail: (1) `OperationsEndpoints.ParseSubmissionAsync` callers now map a bare `InvalidOperationException` from JSON string reads (unpaired surrogate escapes) to 400 `invalidRequest` per API §8 instead of escaping as 500 — shared translation/rewriting path; (2) the generated 201 documents both success envelopes with `anyOf` (minimal APIs collapse duplicate status-code metadata and the OpenAPI layer rejects duplicate keys, so an operation transformer sets the `anyOf` and a document transformer restores the dropped `TranslationSuccessResponse` component by cloning the live-generated rewriting envelope with only the text field renamed; `scripts/openapi-to-yaml.mjs` now enforces the two-variant shape).

## Ordered tasks
- [x] T001 — Reuse review of M026/M017/M018/M022–M025 entry points (operations wire and admission/settlement/recovery/snapshot/monetary services, rewriting pipeline/bundle/parser, chain traversal); AC-001–008 prerequisites; depends on none; done when touched files/behaviors are listed and regressions identified.
- [x] T002 — Rewrite submit/status contract DTOs in `OperationsContract.cs` + OpenAPI generation/review with zero drift; AC-008; depends on T001; done when `scripts/contract.sh` passes and the semantic diff is reviewed.
- [x] T003 — Deterministic fake rewriting provider + fault injection (test composition only, production-inaccessible); AC-001/003/005; depends on T001; done when scripted classification/per-mode/fault paths are asserted through the real HTTP host pipeline.
- [x] T004 — Rewriting coordinator + endpoint wiring (admission → chain → validation → settle-once; identity-state table; deadline fencing; per-attempt monetary admission; fresh snapshot on every response); AC-001–007; depends on T002, T003; done when file-backed SQLite/HTTP suite passes via `scripts/backend.sh`.
- [x] T005 — Privacy/drift closeout (no text/secrets/counts in problems/logs/traces/TEXT columns; `no-store`; contract drift clean) + `python3 automation/context.py check M027`; AC-008; depends on T004; done when checks pass and evidence is recorded below.

## Completion record
Revision: `ed814e0` plus the uncommitted M027 change set (no commit; the runner owns commits). UTC: 2026-09-11T16:35Z at final verification. Environment: .NET SDK 10.0.302, Node v24.20.0, npm 11.11.0.

Touched files/behaviors (T001): `OperationsContract.cs` (TranslationSuccessResponse/TranslationProblemCategories pattern extended), `OperationsEndpoints.cs` (translation branch, ParseSubmissionAsync, identity-state table), `TranslationOperationCoordinator.cs` (mirrored family-scoped), `OperationAdmissionService.cs` (rewriting validation, `correctionOnly` default-before-fingerprint, untouched), `OperationSettlementService`/`OperationRecoveryService`/`MonetaryAdmissionService` (family/mode parameters reused), `ProductCatalog` (`correctionOnly` default, nine modes), `ScalarInputPolicy.ValidateRewriting`, `ChainOrchestrator.ExecuteRewritingAsync` (exists, reused), `RewritingPipeline`/`RewritingEnvelopeParser`/`RewritingPrompt` (`rewriting.v1`), `TranslationClientProvider` (provider pattern mirrored), `OperationsRegistrationExtensions`, `FamilyChain.EvaluationDefault(ChainFamily.Rewriting)`. Regressions identified: translation/auth/accounting/usage suites plus contract drift gate; all green.

Evidence (all over file-backed SQLite with the fake server clock and the deterministic fake rewriting provider unless noted):
- AC-001: `OperationRewritingTests.RewriteSubmitExecutesSynchronouslyWithChargeAndSnapshot` (verified-account rewrite submit with explicit mode returns 201 with complete validated same-language text, charged scalar count, admission day, 30 s deadline and a fresh current-day snapshot with `no-store`; exactly 2 provider dispatches; 2 released monetary reservations with zero known spend and zero unresolved exposure; status read reports succeeded/output-unavailable with the same charge and no new dispatch; usage read consistent) and `RewriteWithoutConfiguredProviderRemainsPending` (unconfigured production composition keeps the 202 pending reservation path).
- AC-002: `RewriteValidationMatrixRejectsWithoutDispatchOrCharge` (malformed, unknown/duplicate fields, unknown mode, combined modes, invalid source selection, empty, whitespace-only, oversized at the 2000 scalar limit, and unpaired-surrogate escapes rejected with 400/422 input categories, zero dispatches, no admitted operation, unchanged ledgers).
- AC-003: `RewriteEligibilityRejectionsEndWithoutTransformationOrCharge` (uncertain/unsupported/mixed/source-mismatch end as 422 eligibility rejections with no transformation dispatch and no character charge; failed status reads report zero charge).
- AC-004: `DuplicateRewriteReplaysMetadataWithoutNewChargeOrDispatch` (same-identity/same-payload replay returns succeeded/output-unavailable metadata with the original charge day, fresh usage, no new dispatch or charge; same-identity/different-payload conflicts with 409; one stored submission and one full-source charge); `OmittedModeDefaultsToCorrectionOnlyAndFingerprintsIdentically` (omitted mode succeeds and replays identically with explicit `correctionOnly`; a mode change conflicts with 409); `CorrectionOnlyUnchangedTextStillSucceedsWithFullCharge` (correction-only pass returning the source unchanged still succeeds with the full-source charge).
- AC-005: `TransientRewriteFailuresExhaustToProcessingFailure`, `InvalidRewriteOutputExhaustsToProcessingFailure` and `RewriteRefusalsExhaustToProcessingFailure` (bounded chain exhausts to terminal 503 processing failure with zero charge; dispatch bounds 2/3/2 observed); `RewriteFallbackReceivesOriginalSourceAndMode` (fallback receives the original complete source with resolved language and mode); `RewriteDeadlineExpiryFencesExecutionWithZeroCharge` (fake-clock advance past the stored deadline ends as terminal 504 with zero charge and no transformation dispatch); `DeniedRewriteMonetaryAdmissionStopsDispatchWithSuspension` (over-cap denial stops dispatch with 503 monetary suspension, no reservation writes, zero charge).
- AC-006: `SettledRewriteSurvivesRestartWithDispatchFreeReads` (settled rewriting success survives same-file restart with output unavailable and the original charge; unknown stays 404 asserting nothing; reads dispatch nothing); pre-commit crash fencing reuses the M024 orphan-scan path (regression suites green, no new pending-execution state introduced).
- AC-007: `ExhaustedUserAllowanceDeniesRewriteWithoutDispatch` (exhausted user allowance denied with 429 userAllowance distinction and UTC-midnight reset guidance, zero dispatch on denial, snapshot reports user-exhausted with the M025 rules unchanged).
- AC-008: `RewriteFailureCarriesNoSourceTextOrSecrets` and `RewriteSuccessOmitsSourceTextOutsideRewrittenText` (sentinels absent from problem bodies and every TEXT-bearing store column; provider internals never leak); `RewriteAndTranslationSuccessEnvelopesStaySymmetric` (both success DTOs share every property/type except the text field, guarding the component restore); `bash scripts/contract.sh check` passes with zero drift on the reviewed delta (201 `anyOf` both envelopes, new `RewritingSuccessResponse` component, translation component byte-identical to M026, regenerated TypeScript union, required no-store on every new response); log templates carry counts/day/dispatches only.

Checks: `bash scripts/backend.sh check` — 498 total (API/storage 217 including 18 new rewriting tests, core 10, independent AI 271), 498 passed, 0 failed; `bash scripts/contract.sh check` — zero drift; `python3 automation/context.py check M027` — selected inputs and spec/plan unchanged after reviewed re-lock. No human/live evidence is required by this slice; none was used.
