# 006 — Register a Local API Account: Tasks

**Version:** 1.2 · **Updated:** 2026-09-09
**State:** Complete; AC-001–008 passed
**Inputs:** [spec.md](spec.md) v1.1; [plan.md](plan.md) v1.1

## 1. Ordered implementation tasks

- [x] **T001 — Reconfirm dependencies and make the selected contract preflight deterministic.** Compare the current tree/sources with planning baseline `4aca2c52`, confirm M003/M005 evidence, SDK/Node/npm/EF pins, locked NuGet/npm access and audits. Apply the observed `--disable-build-servers` remedy to `scripts/contract.sh`'s locked restore, then prove normal setup/check plus controlled failure return promptly/nonzero as appropriate before contract-dependent work. Pin only the compatible Identity EF dependency and update locks; add no auth UI/provider/role/breach-list/runtime-fetch package. **Targets:** `backend/Directory.Packages.props`, API/test project locks, `scripts/contract.sh`. **Depends:** none. **Refs/checks:** BI-006; AC-007/008; indispensable preflight passes or implementation stops with the real blocker.
- [x] **T002 — Add durable Identity schema and Data Protection configuration.** Convert the application context/store registration to standard local Identity with unique normalized email and configured 15–128 scalar/no-composition password policy. Add the explicit absolute key-directory policy with fixed `LinguaDesk` application name. Generate/inspect a new `LocalAccounts` migration/snapshot without editing M003's migration and without adding roles, product profile, ledger, text, outbox or key tables. **Targets:** API project, `Infrastructure/Persistence/`, new security infrastructure, migrations/snapshot. **Depends:** T001. **Refs/checks:** US-003; AC-002/004/006/008; fresh/upgrade/model-drift and key restart checks agree with #3/#5.
- [x] **T003 — Enforce database/key startup and readiness boundaries.** Add non-mutating pre-listen validation for an existing current writable database and usable existing key directory plus OpenAPI-excluded `GET /health/ready`; preserve process-only `/health/live` and health/API/static fallbacks. Separate build-time contract activation without a user-configurable production bypass. Cover missing/stale/locked/unwritable stores, absent/unusable keys, post-start unhealthy state and no auto-create/migrate/delete/fallback. **Targets:** persistence/security/readiness infrastructure, `Program.cs`, host/process test helpers. **Depends:** T002. **Refs/checks:** US-003; AC-006/007/008; V-016 selected readiness and M003 regression assertions.
- [x] **T004 — Declare and review the selected registration wire shape.** Add strict request/response records and endpoint metadata for only `POST /api/accounts/register`/`registerLocalAccount`, typed 202/400/415/503/no-store semantics and anonymous security. Register the under-construction route before the catch-all, advance the pre-release document to `0.1.0-m006`, generate YAML/types from the real entry point and semantically review the two implemented paths before completing the handler. No future Identity route/security scheme/account field may appear. **Targets:** `Features/Identity/Registration/`, OpenAPI registration, `Program.cs`, `docs/05-openapi.yaml`, generated TypeScript declaration. **Depends:** T001/T003. **Refs/checks:** US-001/US-002; AC-001–003/007; API-AC-014 selected slice.
- [x] **T005 — Implement registration, confirmation intent and duplicate handling.** Implement strict JSON/email/password validation, real Identity creation/hash/store and the narrow confirmation sender boundary. Map a new account and sequential/case/concurrent normalized-email duplicates to the identical generic acknowledgment while producing one row/delivery total; map invalid/unavailable failures safely. Keep a new account unverified and unauthenticated. Capturing/failing senders are injectable tests only; delivery exceptions leave the row, expose no membership/secret and never retry. **Targets:** registration slice, DI/configuration, API integration fixtures. **Depends:** T002/T004. **Refs/checks:** US-001/US-002; AC-001–003/005; V-004/V-009/V-015 selected assertions.
- [x] **T006 — Add the current-state verified-account guard and focused verification.** Register named `VerifiedAccount` authorization and load current Identity state for missing/unconfirmed/confirmed fixtures. Add real file-backed Identity/HTTP/Data Protection tests for password scalar boundaries/exact hashing, strict shape, duplicate concurrency, restart, sender failure, Problem Details and response/log/report sentinels. Assert no cookie/token/account ID/email/password/token, provider reference/call or language route; do not add a public test endpoint. Add positive focused-test guards. **Targets:** Identity authorization policy/handler, `LinguaDesk.Api.Tests`, backend check/report guard. **Depends:** T002/T005. **Refs/checks:** US-003; AC-001–005/008; API-AC-003 unverified-state portion.
- [x] **T007 — Run contract and regression verification and update operating truth.** Run the focused migration/registration/readiness tests, locked aggregate backend check, contract generate/check/semantic/type review, frontend check, backend smoke and published-shell smoke with isolated migrated storage/key paths and safe test delivery. Exercise deliberate drift/invalid command/startup failures and inspect generated paths, package/reference graph, processes/filesystem/logs for forbidden effects/secrets. Update README with actual initialization/key/config/check behavior and explicit no-live-email/no-sign-in/no-verification/no-language limitations. **Targets:** scripts/test reports, README, generated artifacts. **Depends:** T003/T004/T006. **Refs/checks:** AC-006–008; current M001–M005 behavior remains green and zero-test/drift failures cannot pass.
- [x] **T008 — Reconcile acceptance and close M006 without overstating access.** Compare the complete diff/artifacts/migration with #0, BI-006, spec, plan, #1/#2/#3/#5/#6 and ADR-003/004/007/009. Record implementation revision/environment, dependency versions/audits, commands/exits/counts/reports, migration IDs, contract semantics, concurrency/restart/readiness/delivery/secret evidence, risks and AC-001–008 dispositions in a completion record. Update #3/#5–#7, backlog/task states and README only where evidence supports it; leave M007–M014/M034, Q-004, P-005/P-006 and RG-005/release evidence pending. **Targets:** this file, backlog, `docs/03-architecture.md`, `docs/05-api-design.md`, `docs/06-verification-plan.md`, `docs/07-roadmap.md`, README. **Depends:** T007. **Refs/checks:** all; mark Done only if every scenario passes, otherwise preserve exact blockers/tasks.

## 2. Scenario-to-task mapping

| Scenario | Tasks | Planned evidence |
| --- | --- | --- |
| AC-001 | T004/T005/T006/T008 | Real Identity HTTP creation/hash/store, 202/no-store body and exactly one captured intent with no auth/secret/account disclosure |
| AC-002 | T002/T004/T005/T006/T008 | Strict JSON/media/email and Unicode 14/15/128/129 cases; field Problem Details and secret-sentinel inspection |
| AC-003 | T002/T004/T005/T006/T008 | Sequential/case/concurrent normalized-email requests produce identical responses and one durable row/delivery |
| AC-004 | T002/T006/T008 | Same-file restart plus current-state policy denial/allow fixtures and no language/provider surface inspection |
| AC-005 | T005/T006/T008 | Capturing/failing sender outcomes, durable unverified row, identical acknowledgment, safe diagnostics and zero retry |
| AC-006 | T002/T003/T007/T008 | Process/readiness matrix for current/missing/stale/locked/unusable database/key targets with no implicit mutation/disclosure |
| AC-007 | T001/T003/T004/T007/T008 | Deterministic two-path OpenAPI/types, exact semantics, drift/failure guards and no-effect route/generation inspection |
| AC-008 | T001–T008 | Migration/model/check/report guards, audits, full regressions and final source/scope/evidence reconciliation |

## 3. Completion record

**Implementation status:** Complete on the working tree based on planning HEAD `6b4055d27396aef91608d8770122daa7f7ea9b48`; the user owns the eventual commit. **Runtime scenario status:** AC-001–008 passed. **Human action/blocker:** None. **Environment:** macOS 26.6.2 / Darwin 25.6.0 arm64, .NET SDK 10.0.302, Node 24.20.0 and npm 11.11.0 on 2026-09-09.

The implementation pins `Microsoft.AspNetCore.Identity.EntityFrameworkCore` 10.0.10, adds the inspected user-only `20260909120834_LocalAccounts` migration without changing `20260908221711_InitialStorage`, and retains repository-local `dotnet-ef` 10.0.10. `dotnet list backend/LinguaDesk.slnx package --vulnerable --include-transitive` and `npm --prefix frontend audit --audit-level=moderate` both exited 0 with no vulnerable package reported. Package/reference inspection found Identity EF only in the API dependency graph and no role, email-provider, breach-list, auth-UI or provider SDK addition.

Verification evidence:

- `bash scripts/backend.sh check` passed 87 tests: 67 API/storage/Identity/readiness, 10 Core and 10 Infrastructure.Ai. The API report includes at least 10 registration, 9 readiness and 7 migration cases, and the script fails if any focused or aggregate suite discovers zero/too few tests. TRX files are under `artifacts/test-results/`.
- `bash scripts/backend.sh smoke` explicitly migrated an isolated database through `InitialStorage` and `LocalAccounts`, used an isolated existing key directory, and passed both process-only liveness and dependency readiness before cleaning its owned process/files.
- `bash scripts/contract.sh check` passed deterministic generation from the real entry point and TypeScript compilation. A deliberate checked-in YAML version drift returned 1 with `OpenAPI contract drift detected`; `bash scripts/contract.sh invalid` returned 2 with usage. The canonical artifacts were then regenerated and rechecked. SHA-256: `58155b6c9c79fcf3035ae7515d606bee19a43c6afbd9bb116b76c6f32b5d2881` for `docs/05-openapi.yaml`; `eb9723abef6b7a6eceab0d24b6625a429a028fe2bfb95c4679ca98c6bfd0ea1f` for the TypeScript declaration.
- `bash scripts/frontend.sh check` passed typecheck, lint, build and 36 unit/component cases. `bash scripts/frontend.sh smoke` republished the application and passed 6 isolated Chromium cases with explicitly migrated storage and key configuration.
- Focused HTTP/file-backed cases proved strict JSON/media/query/email/password scalar rules; exact accepted-password hashing; generic sequential, case-variant and concurrent duplicate responses with one row/one delivery; durable unconfirmed restart state; controlled delivery failure with no retry; and current-state missing/unconfirmed/confirmed policy outcomes. Response/source/log sentinels found no submitted email/password, account identifier, credential or confirmation material exposure.
- Readiness/startup cases proved current writable migrated storage and a usable existing key directory pass; missing/stale/locked/post-start-lost database and absent/post-start-lost/unsafe key targets fail or become unhealthy without auto-create/migrate/delete/fallback. Liveness stays dependency-free. Contract generation skips serving validation and Data Protection construction while retaining actual route metadata, so it opens no database, writes no key, sends no email and starts no listener.
- `bash -n scripts/backend.sh scripts/contract.sh scripts/frontend.sh` and `git diff --check` passed. The final generated document is OpenAPI 3.1, version `0.1.0-m006`, and contains exactly `GET /api/capabilities` plus anonymous `POST /api/accounts/register` with the selected request, 202/400/415/503 and `Cache-Control: no-store` shapes; `/health/ready` is excluded.

**Acceptance disposition:** AC-001 passed by real Identity creation/hash/store and generic 202 evidence; AC-002 by validation and scalar-boundary cases; AC-003 by duplicate/concurrency evidence; AC-004 by restart and current-state authorization; AC-005 by capture/failure delivery cases; AC-006 by the startup/readiness failure matrix; AC-007 by deterministic generation, route-boundary and failure guards; AC-008 by audits, aggregate regressions, source review and authority reconciliation.

M006 does not satisfy full FR-001/002 or RG-005. Sign-in, confirmation/resend/status/reset, a frontend account flow, real email, language work, accounting, deletion/backup lifecycle, P-005/P-006 and every release gate remain pending with their named owners.

**Post-completion development-run correction — 2026-09-09:** A real `backend.sh run` attempt exposed that the wrapper inherited the production defaults and failed with a low-level missing-key-directory stack trace before a local developer had prepared M006 dependencies. The wrapper now explicitly migrates a persistent database and provisions a restricted key directory under an overridable absolute local-development directory when the corresponding runtime settings are absent. Explicit custom database/key settings retain fail-closed application behavior and are never auto-created by the host. A real defaulted wrapper run passed startup plus liveness/readiness, and the full backend/contract/frontend checks remained green.
