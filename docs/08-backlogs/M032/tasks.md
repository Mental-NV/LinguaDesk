# M032 — Tasks and evidence
Inputs: [spec](spec.md), [plan](plan.md).

## Resume
Next: done — all tasks complete, milestone Done. Blockers: none.
Last check: `bash scripts/verify-milestone.sh M032` passes (2026-09-11T18:29Z, repair rerun after a transient runner-side `dotnet restore --locked-mode` SIGSEGV in `run.QtmwW9/M032-verification-1.log`; see repair note in the completion record). Changed scope: none (no source, lock, or assertion changes in the repair).

## Ordered tasks
- [x] T001 — Add `workspaceCleared` reducer action in `translationWorkspace.ts` and `rewritingWorkspace.ts` (defaults restoration, usage-snapshot preservation, late-response fencing); AC-002/AC-005/AC-006; depends on none; done when V-002 pure units pass.
- [x] T002 — Add store `resetAll` in `workspaceStores.ts` and wire sign-out/expiry/reset-confirm teardown through it (abort paid + read-only flights, clear saved scroll); AC-005/AC-006; depends on T001; done when store/fencing units pass.
- [x] T003 — Build the §3.4 native reset dialog with inline `Start new workspace` on both pages, empty-workspace bypass, confirm clearing + `/translate` navigation + Source-text focus, footer copy, autocomplete/storage exclusions; AC-001/AC-002/AC-003; depends on T002; done when V-003 component tests pass with zero operation posts on reset.
- [x] T004 — Add `pagehide` clearing + defensive `pageshow` reset on workspace routes and storage/URL/history/cache audit; AC-004/AC-008; depends on T003; done when focused V-010 browser contracts prove real restoration clearing.
- [x] T005 — Publish dedicated-user E2E reset/teardown/restoration cases (dialog, empty bypass, pending-confirm fencing, reload, sign-out incl. failure, expiry MSG-037) with ≥1 real `/login` form path, real-auth fixture reuse, semantic assertions, Smoke-only seeding and password-absence scan; AC-001–AC-007; depends on T004; done when published suite passes and scan is clean.
- [x] T006 — Prove privacy on new paths and run V-002/V-003/V-010 focused checks, `contract.sh check` zero drift, and M028–M031 regression suites; AC-008/AC-009; depends on T005; done when sentinel inspection, focused suites, drift check and regressions pass.

## Completion record
Repair 2026-09-11T18:29Z: the runner's `run.QtmwW9/M032-verification-1.log` failed at `bash scripts/backend.sh check` when `dotnet restore backend/LinguaDesk.slnx --locked-mode` died with SIGSEGV (exit 139) after a successful `dotnet tool restore` — same signature as the earlier verification-0 transient. Diagnosis unchanged: transient .NET SDK crash, not a product or lock defect — the M032 change set is frontend-only (`git diff HEAD -- backend/` empty), and a retry of the identical locked restore in this session exited 0 with "All projects are up-to-date for restore" (.NET SDK 10.0.302). No relock, no source/assertion edit, no gate skipped. Full `bash scripts/verify-milestone.sh M032` then passed end to end (context check + audit, backend check + smoke, contract check, ai check + probe, frontend check + smoke incl. 61 published cases with 6 new workspace-reset cases, `git diff --check`), closing with "[verification] M032 passed all milestone regression gates."
Repair 2026-09-11T18:26Z: the runner's `run.QtmwW9/M032-verification-0.log` failed at `bash scripts/backend.sh check` when `dotnet restore backend/LinguaDesk.slnx --locked-mode` died with SIGSEGV (exit 139) after a successful `dotnet tool restore`. Diagnosis: transient .NET SDK crash, not a product or lock defect — the M032 change set is frontend-only (no backend, migration, DTO, OpenAPI, or lockfile change; `git diff HEAD -- backend/` empty), and a retry of the identical locked restore in this session exited 0 with "All projects are up-to-date for restore". No relock, no source/assertion edit, no gate skipped. Full `bash scripts/verify-milestone.sh M032` then passed end to end (context check + audit, backend check + smoke, contract check, ai check + probe, frontend check + smoke incl. 61 published cases with 6 new workspace-reset cases, `git diff --check`), closing with "[verification] M032 passed all milestone regression gates."

Executed 2026-09-11T18:22Z on .NET SDK 10.0.302 / Node v24.20.0 / npm 11.11.0, working tree at `686d85a` plus the uncommitted M032 diff (runner owns commits). Environment: local run; deterministic Smoke provider; published host on owned HTTPS loopback with a fresh migrated SQLite seed (`m013-verified@example.test`, usage 7,500) removed afterward.

- T000: M031 Done confirmed in delivery/current.md; `python3 automation/context.py check M032` clean at start (18 sources unchanged); working tree clean; current code kept both workspaces in the lifted store with abort-only teardown and no reset action, dialog, footer or restoration wiring (greenfield slice on the M030 store).
- T001: both workspace reducers gain `workspaceCleared`: FR-038 defaults restored (translation auto/empty-target, rewriting Correction only), settled usage snapshot and loaded capabilities preserved, `appliedRevision` reset, `requestRevision` bumped so late `submitSucceeded` can only record its independently settled charge (never restores text) while late failures/status answers are ignored. Reducer units (4 new per family) pass.
- T002: each feature store gains `resetWorkspace` (aborts paid + read-only flights, drops saved scroll, dispatches the clearing action) and the provider gains `resetAll` clearing both features. `App.tsx` sign-out now funnels through `resetAll` (immediate clear, M013 failure/redirect semantics unchanged) and the session-expiry bootstrap clears lifted text before the login form renders with MSG-037. Store units (2 new) pass, including a gated-submit reset with a past-the-abort dispatch restoring no text.
- T003: `WorkspaceReset.tsx`/`workspaceResetModel.ts` add the inline `Start new workspace` button on both pages, the exact §3.4 native dialog (heading/body/buttons, Cancel autofocus, Escape/Cancel preservation with trigger-focus return), empty-workspace immediate reset, confirm clearing both pages with `/translate` navigation + Source-text focus and zero operation posts, the footer lifetime copy on both pages, and `autoComplete="off"` on all four workspace editors. Component tests (6 new) pass; existing page/store suites render under MemoryRouter since pages now own navigation.
- T004: `WorkspaceSessionTeardown` on both workspace routes clears on `pagehide` and defensively resets on persisted `pageshow`; SPA mode links fire neither event so UX-AC-002 preservation holds. Source audit: no localStorage/sessionStorage/IndexedDB/service-worker/HTTP-cache/history text paths exist; editors are autocomplete-excluded. Handler-level jsdom proof plus real-restoration proof in the published suite (synthetic dispatch is handler-only evidence, as labeled).
- T005: `frontend/tests/e2e/workspace-reset.spec.ts` (6 cases) reusing the documented real-auth fixture: real `/login` form case covering dialog copy/focus, Escape preservation and confirm clearing of both pages with usage preserved and zero operation posts; empty immediate-reset case; reload/full-document-navigation/goBack-restoration/second-tab case with storage/URL checks; sign-out immediate-clear with Back-clean case; expired-session MSG-037 case with sign-back-in to an empty workspace; footer + sentinel-residue case on both pages. All 61 published cases pass (55 pre-existing + 6 new); the smoke password scan is clean.
- T006: AC-008 sentinel inspection: change set is frontend-only (no backend, migration, DTO or TEXT-column change); no new console/storage/clipboard sinks and no secret material in the diff; E2E sentinel/storage/URL assertions pass. V-002/V-003 pass inside `frontend.sh check` (261 tests, +16); V-010 browser-native assertions (clipboard/editing/caret in M028/M029 paths, reload/navigation/restoration here) pass inside the smoke run. `contract.sh check` zero drift (no wire-shape change, as selected); M028–M031 published suites rerun as regressions inside the same smoke run (all pass; their scope and evidence unchanged).

AC mapping: AC-001 (dialog/focus/bypass) by T003 component tests plus T005 E2E dialog case; AC-002 (confirm with pending ops) by T001/T002 fencing units plus T005 E2E confirm clearing with zero operation posts (late-response fencing strictly proven with deferred responses at unit level; the deterministic Smoke adapter settles synchronously); AC-003 (empty immediate reset) by T003 component plus T005 E2E empty case; AC-004 (real restoration) by T004 teardown plus T005 E2E reload/navigation/restoration case; AC-005 (sign-out teardown) by T002 wiring, M013 unit failure-copy proof and T005 E2E success/Back case; AC-006 (session expiry) by T002 wiring plus T005 E2E MSG-037 case; AC-007 (E2E auth contract) by the real `/login` case, fixture reuse, Smoke-only seeding and clean password scan; AC-008 (privacy) by T004/T006 inspection plus E2E sentinel/storage/URL cases; AC-009 (zero drift) by T006.

Limitations. The transient `Signing out…` state is asserted only in jsdom units (M013 gated-response proof); the published sign-out case asserts the immediate clear and settled form because the loopback POST settles faster than the assertion poll. In-SPA session expiry without reload has no app trigger (no polling); expiry teardown is proven at the bootstrap gate where the login form renders. Tab-close clearing is memory-lifetime evidence, not directly automatable; reload/navigation/restoration/second-tab cases cover the observable teardown. Seed usage assumes seed and run share a UTC day. Visual baselines and browser/device/AT claims stay with M038/M039/G3.
