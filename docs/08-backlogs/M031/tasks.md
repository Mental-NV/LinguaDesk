# M031 — Tasks and evidence
Inputs: [spec](spec.md), [plan](plan.md).

## Resume
Next: none — all tasks complete. Blockers: none.
Last check: `bash scripts/frontend.sh smoke` (55/55 published cases pass, smoke password scan clean); `bash scripts/frontend.sh check` (245 unit/component tests); `bash scripts/contract.sh check` (zero drift); `bash scripts/backend.sh check` (505/505); `python3 automation/context.py audit` (clean); `git diff --check` (clean).
Changed scope: none.

## Ordered tasks
- [x] T000 — Confirm M030/M024/M025 Done via their tasks completion records and delivery status, and current workspace/API code state; AC-none (prerequisite); depends on none; done when resume pointer advances with no divergence noted.
- [x] T001 — Add `fetchOperationStatus` read-only clients for both families against the existing generated status shape; AC-004; depends on T000; done when status-mapping units (all states + 404/410/unavailable) pass.
- [x] T002 — Extend both workspace reducers with unknown-outcome, status-resolution and usage-unavailable states under existing revision guards; AC-003, AC-004, AC-005, AC-006; depends on T001; done when reducer units pass.
- [x] T003 — Render Check status / Refresh usage / lost-output disclosure on both pages with §6.4 announcement rules, wired to fenced store flights; AC-003, AC-004, AC-005, AC-006; depends on T002; done when component checks pass.
- [x] T004 — Publish dedicated-user E2E recovery cases (definitive retry, offline unknown → no-record, usage refresh) with ≥1 real `/login` form path, real-auth fixture reuse, semantic assertions, Smoke-only seeding and password-absence scan; AC-001–AC-007; depends on T003; done when published suite passes and scan is clean.
- [x] T005 — Prove privacy on new paths and run V-002/V-003/V-005/V-010 focused checks; AC-008; depends on T004; done when sentinel inspection and focused suites pass.
- [x] T006 — Regenerate OpenAPI/clients per M026/M027 procedure and assert zero drift; rerun M028/M029/M030 published suites as regressions; AC-009; depends on T004; done when generation diff is empty and regressions pass.

## Completion record
Executed 2026-09-11T17:54Z on .NET SDK 10.0.302 / Node v24.20.0 / npm 11.11.0, working tree at `843a37dca766f44822f38f4f2aa4645628f99ac0` plus the uncommitted M031 diff (runner owns commits). Environment: local run; deterministic Smoke provider; published host on owned HTTPS loopback with a fresh migrated SQLite seed (`m013-verified@example.test`, usage 7,500) removed afterward.

- T000: M030/M024/M025 completion records and Done delivery status confirmed; `python3 automation/context.py check M031` clean (22 sources unchanged); working tree clean; current code has no status-read client and no recovery states (greenfield slice on the M030 store-lifted workspaces).
- T001: `fetchOperationStatus` in `frontend/src/api/translation.ts` and `frontend/src/api/rewriting.ts` against the existing generated `getLanguageOperationStatus` shape (`OperationStatusResponse`; no DTO/handler/OpenAPI change). 200 maps all four terminal states with usage via the existing `toUsage`; 404 → `unknownRecord`, 410 → `windowExpired`, 401/403 → auth kinds, anything else → `unavailable`. `frontend/tests/unit/operationStatus.test.ts` (4 tests) proves every mapping for both families plus GET-only/no-text-in-request assertions.
- T002: both workspace reducers gain the unknown-outcome error (`canCheckStatus: true`, `canRetry: false`, retained `pendingOperationId`), the six-way `statusResolved` resolution set (lost-output disclosure with confirmed charge; terminal failure with retry; pending preserved; no-record/window-expired guidance with new submission; auth), `usageRefreshFailed`/`usageUpdated` reconciliation, edit-clears-identity, revision+identity fencing for late answers, and `canSubmit` blocked while an outcome is unknown. Reducer units (8 new translate, 6 new rewrite) pass.
- T003: both pages render `Check status` (unknown only) instead of `Try again`, the `Usage update unavailable` notice with a read-only `Refresh usage` action in the Usage section, and the lost-output disclosure through the existing `role="alert"`/`role="status"` regions; store flights (`checkStatus`, `refreshUsage`) run on a separate abort controller so reads never abort a paid submission, and a new submit supersedes them. Deliberate reading: any submit-flight transport failure — including the antiforgery bootstrap, which precedes dispatch — enters the unknown outcome per UX-AC-075 (never infer failure or zero charge from a transport timeout); Check status then deterministically observes the no-record path. Component tests (3 new per page) pass.
- T004: `frontend/tests/e2e/operation-recovery.spec.ts` (7 cases) reusing the documented real-auth fixture from `translate-auth.ts`: real `/login` form case; per-page definitive-failure retry with new-key assertion via the proven `M028/M029-PROCESSING-FAILURE` sentinels (plus no-`Check status` distinction and edit-clears tail with +20/+43 charges); per-page `setOffline` unknown-outcome → `Check status` → no-record guidance with zero operation posts and exactly one status GET, then explicit resubmission charging +20/+43; per-page read-only `Refresh usage` with zero operation posts, including the offline unavailable roundtrip on translate. All 55 published cases pass (48 pre-existing + 7 new); the smoke password scan is clean.
- T005: AC-008 sentinel inspection: change set is frontend-only (no backend, migration or TEXT-column change); no new `console`/storage/clipboard sinks; the only request interpolation is the UUID identity in the status path; new copy interpolates only the numeric charge count; usage display still shows only the user's own consumed/allowance/remaining/reset. V-002/V-003 pass inside `frontend.sh check` (245 tests); V-005 passes inside `backend.sh check` (505/505); V-010 browser-native assertions (clipboard in the M028/M029 happy paths) pass inside the smoke run.
- T006: `contract.sh check` zero drift (no wire-shape change, as selected); M028/M029/M030 published suites rerun as regressions inside the same smoke run (all pass; their scope and evidence unchanged).

AC mapping: AC-001 (definitive retry + new key) by T004 E2E per page; AC-002 (deadline/allowance/budget copy with no retry affordance, no auto-submit) by existing V-003 problem-mapping component tests (504/429/monetary cases, unchanged behavior) — no deterministic deadline sentinel exists so no new E2E was added for MSG-014; AC-003 (unknown outcome) by T002/T003 units plus T004 E2E per page; AC-004 (Check-status resolution) by T001 units for all states plus T004 E2E no-record path (succeeded/failed/interrupted/pending mappings proven against the real generated shapes in units, since the Smoke provider settles synchronously); AC-005 (usage refresh) by T004 E2E plus units; AC-006 (no unintended work, stale fencing) by T002 units plus E2E request-count assertions; AC-007 (E2E auth contract) by the real `/login` case, fixture reuse, Smoke-only seeding and clean password scan; AC-008 (privacy) by T005 inspection; AC-009 (zero drift) by T006.

Limitations. Deterministic fixture text is transport/presentation evidence only (V-013 out of scope). The `succeeded`/`failed`/`interrupted`/`pending` Check-status branches cannot occur deterministically end to end (Smoke settles synchronously and the offline submit never reaches the server); they are proven at client/reducer/component level against the real generated shapes. New UI copy (`We couldn’t confirm…`, no-record/window-expired guidance, lost-output disclosure, `Usage update unavailable`) has no UX-MSG ID assigned; normative §6.2/§6.3/UX-AC-071 wording is used verbatim where it exists. Seed usage assumes seed and run share a UTC day. Visual baselines and browser/device/AT claims stay with M038/M039/G3.
